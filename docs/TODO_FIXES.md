# TODO_FIXES — Corrections à apporter

> 🗄️ **ARCHIVÉ 2026-05-25 — Backlog actif désormais dans Notion**
>
> Ce fichier est conservé pour l'**audit historique** (162 items résolus = ~95% de la dette technique trackée 2026-05-03 → 2026-05-23). Les 2 derniers fixes restants (`GoalsTabViewModel` auto-completion + `DrawerMiniProgress` track invisible) ont été migrés dans le Kanban Notion.
>
> **Source vivante du backlog sport-app** :
> - Kanban : https://www.notion.so/1823c95d613a4c08882d5a75d3dda714
> - Page projet : https://www.notion.so/36b1776cd0e2815b9817d9796291402f
>
> Pour ajouter une tâche : `/add-task <description>` (slash command Claude Code) ou directement dans Notion. Pour démarrer une tâche : `/start-task` (orchestré par agent `po-sport`). Voir [CLAUDE.md](../CLAUDE.md) pour le workflow agents complet.

> ⚠️ **LISTE STABILISÉE AU 2026-05-23 — 99% RÉSOLU** (1 item restant / 162 originaux : `GoalsTabViewModel` auto-completion §5). Tous les autres fixes sont tickerés (ops + refactors + bugs livrés). La barre fix-first est largement franchie. Pour les vagues de fixes postérieures (et l'avancement réel), voir l'historique de [CLAUDE.md](../CLAUDE.md) et [CHANGELOG.md](CHANGELOG.md).

> Liste exhaustive des corrections / nettoyages identifiés durant l'audit (étapes 1 → 5). Réorganisée par **axe technique** pour faciliter le passage à l'action. Pour les **nouvelles fonctionnalités / améliorations** voir [TODO_FEATURES.md](TODO_FEATURES.md).
>
> **Méthode de refonte (étape 6, 2026-05-04)** : refactorisation totale demandée par l'utilisateur. Tous les items des étapes 1/2A/2B-2/2C/3A/3B/3C/3D/4/4.5/5 ont été regroupés par axe technique, dédoublonnés (même bug noté à plusieurs étapes → fusionné), criticités harmonisées (un bug confirmé en pratique gagne sur la version théorique).
>
> **Resync 2026-05-06** : audit ticking de tous les items résolus en V4.x→V8.x dont le `[x]` n'avait pas été reporté ici (le ticking était souvent fait dans CLAUDE.md historique mais pas ici). +50 items mis à jour. Cf. mémoire `feedback_fix_first_priority.md` (politique projet : on ne passe aux features que quand les fixes sont vidés ou explicitement différés).
>
> Légende criticité : 🔴 critique · 🟠 important · 🟡 nice-to-have · ⚪ cosmétique
> Légende statut : `[ ]` à faire · `[~]` en cours · `[x]` fait

---

## Index synthétique — matrice par axe (post-resync 2026-05-06)

| Axe | Restant | Original | Notes |
|---|---|---|---|
| §1 [Sécurité](#1--sécurité) | **0** ✅ | 19 | Section vidée. [V1.2 ops Pi, 2026-05-11] résolu : rotation secrets `change-me` (Postgres + JWT_SECRET_KEY) exécutée manuellement par user via SSH (cf. CLAUDE.md historique). [vagues F1+T1.1.c-bis, 2026-05-06] résolu : audit Type A/C, `get_current_user` dict (obsolète V6.6-B), `ws_router` crash 500 ; **V2.4 cascade ownership pratique** validé en T1.1.c-bis (5 tests pytest cross-user verts, 1 par entité feuille) + bug bonus `upsert_actual_workout_set_by_uuid` UniqueViolation 500 → 403 fixé. |
| §2 [Stabilité production](#2--stabilité-production) | **0** ✅ | 3 | Section vidée. [vague F9-Q5, 2026-05-06] résolu : `exec_pg.py` scindé en `setup_db.py` (idempotent) + `reset_db.py` (destructif avec confirm prompt interactif). Lancement uvicorn découplé. |
| §3 [Sync (REST + WebSocket + dates)](#3--sync-rest--websocket--dates) | **0** ✅ | 26 | Section vidée. [vague F8-A1, 2026-05-06] résolu : `RemoteDataUpserter` push User retiré (politique sécurité, User read-only client). |
| §4 [Schéma DB (Postgres + Pydantic + Room)](#4--schéma-db-postgres--pydantic--room) | **0** ✅ | 31 | Section vidée. [vague F8, 2026-05-06] résolu : `actual_workout_exercises.reps` default "0-1" via Room migration v12→v13 (copy-table), `User` Room timestamps cleanup (Q1 — drop synced/pendingDeletion via Room migration v12→v13), `AvailableEquipment` Type C → Type A user-scoped (Q2 — Alembic `f8q2_ae_user_id` + Room migration). |
| §5 [Architecture / Squelette uniforme / Refactor](#5--architecture--squelette-uniforme--refactor) | **1** | 45 | Reste : GoalsTab auto-completion (refactor logique métier). [B4 chrono, 2026-05-10, P2.6 `e4408a3`] résolu : `ChronoViewModel.onTimerFinished` hardcode `"Rest", 90` → passe par `TimerStateMachine.name` + `timer.durationMillis.value / 1000`. [B3-2, 2026-05-07] résolu : 2 ViewModels VIDES (StatsViewModel implémenté + HistoryViewModel supprimé en B3-1). [vague F8, 2026-05-06] résolu : `Base.metadata.create_all` vs Alembic (Q3 — politique 16 CLAUDE.md hybride documenté), `exercise_crud.bulk_upsert_exercises` raise duplicate (Q4 — UNIQUE(user_id, name) Alembic + check Python retiré). |
| §6 [Code mort / Cleanup](#6--code-mort--cleanup) | **0** ✅ | 18 | Section vidée. [vague F10, 2026-05-06] résolu : audit endpoints serveur orphelins terminé cas par cas — 2 endpoints supprimés (`/superset-exercises/group/{group_uuid}` + `/planned-workouts/{}/exercises`) + 1 path renommé canonique (`/planned-workout-exercise/by-uuid/{}` → `/planned-workout-exercises/{uuid}`) + 1 ticking (PUT /users obsolète V1.1) + 1 keep par squelette canonique (GET /muscle-goals/{uuid}). |
| §7 [Documentation](#7--documentation) | **0** ✅ | 9 | Section vidée. [vague F6, 2026-05-06] résolu : politique traçabilité (CLAUDE.md §14), bug seed_database 5 muscles |
| §8 [Tests existants](#8--tests-existants) | **0** ✅ | 3 | Section vidée. [vague F6-3, 2026-05-06] résolu : inspect_schema.py engine async + emojis cp1252 |
| §9 [UX](#9--ux) | **1** | 4 | Reste : `DrawerMiniProgress` track invisible (bg/recessed sur parent bg/recessed). Découvert 2026-05-23 (build Figma M11 variant WithProgress). [B2, 2026-05-07] résolu : 22 snackbars `safe*WithSnackbar` → résolu par T4.2 + cleanup orphelins (62 lignes mortes supprimées). Le snackbar global vit dans `SyncManager.syncAllToServer()`. [F11-cleanup, 2026-05-06] : ChronoViewModel notif "Rest 90s" doublon strict avec §5 ligne 308 (résolu avec B4 chrono refactor). |
| §10 [Performance](#10--performance) | **0** | 4 | ✅ Section vidée. [vagues F4c+F4d, 2026-05-06] résolu : 28 indexes Postgres (`user_id` × 12 + `<parent>_uuid` × 16) via Alembic + 3 indexes Room (`Notification.created_at`, `Notification.(user_id, created_at)`, `User.username` UNIQUE) via MIGRATION_10_11 |
| **Total** | **1** | **162** | **161 items résolus / 162** (99%) |

> **Note** : ~210 items bruts collectés sur 9 étapes → ~162 items après dédoublonnage initial. Le compte "Restant" recompte les `- [ ]` en racine (sub-bullets aussi mais marginal). Les criticités par item restent visibles dans chaque section (sous-headings 🔴/🟠/🟡/⚪).

---

## Table des matières

- [§1 Sécurité](#1--sécurité)
- [§2 Stabilité production](#2--stabilité-production)
- [§3 Sync (REST + WebSocket + dates)](#3--sync-rest--websocket--dates)
- [§4 Schéma DB (Postgres + Pydantic + Room)](#4--schéma-db-postgres--pydantic--room)
- [§5 Architecture / Squelette uniforme / Refactor](#5--architecture--squelette-uniforme--refactor)
- [§6 Code mort / Cleanup](#6--code-mort--cleanup)
- [§7 Documentation](#7--documentation)
- [§8 Tests existants](#8--tests-existants)
- [§9 UX](#9--ux)
- [§10 Performance](#10--performance)
- [Mapping vers les étapes de l'audit](#mapping-vers-les-étapes-de-laudit)

---

## §1 — Sécurité

### 🔴 Authentification / Secrets

- [x] **Mots de passe `change-me` sur la Pi en prod** — ✅ V1.2 livré 2026-05-11 (ops manuelle SSH par user). Password Postgres alphanum 32 chars + `JWT_SECRET_KEY` hex 64 chars rotés via `ALTER USER fittracker WITH PASSWORD '...'` + création `.env` Pi (`chmod 600`). `sudo systemctl restart sportapi.service` (sudoers NOPASSWD T3.4) OK. Smoke `curl /openapi.json` + `POST /api/v1/token will/<password>` OK -- nouveau JWT signé avec le nouveau secret. Tous les anciens JWT invalidés -> re-login obligatoire S21+ (1 fois, géré par Authenticator OkHttp V4.5).
- [x] **`SECRET_KEY = "MYSECRET123"` hardcodé** ([main.py:121](../serveur/app/main.py#L121)) — utilisé en query param `?key=...` par `/secure-docs` mais **jamais validé côté serveur**. Faux mécanisme. Soit retirer, soit implémenter correctement. ✅ commit `e9a5923` (2026-05-04 QW)
- [x] **JWT `JWT_ISS` / `JWT_AUD` jamais validés au décodage** — déclarés dans `settings.py` et passés à `jwt.decode` sans `audience=` ni `issuer=`. À corriger dans [auth.py](../serveur/app/auth.py). ✅ commit `28df94c` (2026-05-04 QW, breaking : re-login requis)
- [x] **~~Token JWT en clair dans SharedPreferences~~** — ✅ V8.2 Batch 3 (commit `aaf6038`, 2026-05-06) : migration vers `EncryptedSharedPreferences` (AES256_GCM via Android Keystore) + migration silencieuse legacy.

- [x] **Auditer Type A (user-scoped) vs Type C (global) sur toutes les entités** — demande utilisateur 2026-05-06. Rapide vérification des modèles SQLAlchemy : présence du champ `user_id` => Type A. Lister les entités globales (Type C, modifiables admin uniquement, lisibles par tous) vs personnelles. Connue à ce jour : `equipment` + `available_equipment` = global, le reste = user-scoped (`exercise`, `muscle`, `actual_workout*`, `planned_workout*`, `routine_*`, `superset_*`, `training_cycle`, `cycle_workout`, `muscle_goal`, `notification`). À reconfirmer + documenter dans `docs/SERVEUR.md`. Cas suspect : `available_equipment` global semble incohérent sémantiquement (devrait être "ce que CE user a chez lui") — décider si bug ou intentionnel. ✅ vague F1d (2026-05-06) — `docs/SERVEUR.md §3 archétypes` mis à jour avec inventaire confirmé : 11 Type A + 7 Type B (cascade) + 1 Type C ferme (`equipment`) + 1 Type C suspect (`available_equipment` → arbitrage feature). `training_cycle` confirmé Type A depuis V5.7 ; `muscle` confirmé Type A pur (user_id NOT NULL).

### 🔴 Routers publics (politique sécurité violée)

- [x] **`user_router` ENTIÈREMENT PUBLIC** ([user_router.py](../serveur/app/routers/user_router.py)) — aucun `Depends` sur aucune route. N'importe qui sur Internet peut lister/créer/modifier/supprimer tous les users. **Bug critique exposé sur `<public-dns>`**. Sécuriser : `Depends(require_admin)` partout sauf le POST de création (cf. TODO_FEATURES "endpoint signup"). ✅ commits `661b8f2` + `562a021` (2026-05-05)
- [x] **`equipment_router` SANS AUTH** ([equipment_router.py](../serveur/app/routers/equipment_router.py)) — Type C global. Reads → `Depends(get_current_user_id)`, writes → `Depends(require_admin)`. ✅ commit `ec2f7a1` (2026-05-05)
- [x] **`available_equipment_router` SANS AUTH** ([available_equipment_router.py](../serveur/app/routers/available_equipment_router.py)) — `DELETE /available-equipments` (sans uuid) **clear toute la table** sans authentification. ✅ commit `1d2368d` (2026-05-05, Type C provisoire)
- [x] **`training_cycle_router` SANS AUTH** ([training_cycle_router.py](../serveur/app/routers/training_cycle_router.py)) — Type C : reads → auth, writes → admin. 8 endpoints. ✅ commit `b894d8e` (2026-05-05)

### 🔴 Bypass ownership (CRUDs / schémas / routers)

- [x] **`delete_actual_workout` sans check ownership** ([actual_workout_crud.py:88-97](../serveur/app/crud/actual_workout_crud.py#L88)) — signature `(db, workout_uuid)` sans `user_id`. Un user authentifié peut supprimer le workout d'un autre. Aligner sur canonique `delete_X(db, uuid, user_id)`. ✅ commit `234769d` (2026-05-05)
- [x] **`upsert_actual_workout` check ownership conditionnel** ([actual_workout_crud.py:38-59](../serveur/app/crud/actual_workout_crud.py#L38)) — `if "user_id" in workout_data and workout.user_id != workout_data["user_id"]`. Si client n'envoie pas `user_id`, n'importe qui peut updater. Forcer le check via `Depends(get_current_user_id)`. ✅ commit `095f572` (2026-05-05)
- [x] **`upsert_many_actual_workouts` typage faux + pas de check** ([actual_workout_crud.py:62-84](../serveur/app/crud/actual_workout_crud.py#L62)) — prend `list[ActualWorkoutOut]` (schéma de **sortie**) au lieu de `Create`. Aucun check user_id. ✅ commit `d22031a` (2026-05-05, refondu en délégation au singleton)
- [x] **`exercise_muscle_crud.upsert_exercise_muscle` reçoit `user_id` mais l'ignore** ([exercise_muscle_crud.py:45-69](../serveur/app/crud/exercise_muscle_crud.py#L45)) — commentaire ligne 55 "Pas de champ user_id... pas besoin de vérifier propriétaire". Faux. Le helper `assert_user_owns_exercise` existe déjà mais n'est appelé que pour `delete_*` et `get_*`. À ajouter à `upsert_*`. ✅ commit `2c5858a` (2026-05-05, upsert + delete_*_by_uuid)
- [x] **`PlannedWorkoutBase.user_id: int` obligatoire dans `Base`** ([planned_workout_schema.py:8](../serveur/app/schemas/planned_workout_schema.py#L8)) — le client peut envoyer un `userId` différent du sien → l'objet est créé pour un autre user. Idem `PlannedWorkoutCreate`. **Mettre `user_id` uniquement dans `Out`**. ✅ commit `13f3038` (2026-05-05)
- [x] **`SupersetGroupBase.user_id: int` obligatoire dans `Base`** ([superset_group_schema.py:10](../serveur/app/schemas/superset_group_schema.py#L10)) — même bug. À déplacer dans `Out`. ✅ commit `c1981f9` (2026-05-05)
- [x] **3 routers bulk acceptent `list[XOut]`** au lieu de `Create` (propage le bug CRUD au niveau API) :
  - `actual_workout_router.bulk` ([:40](../serveur/app/routers/actual_workout_router.py#L40))
  - `actual_workout_set_router.bulk` ([:40](../serveur/app/routers/actual_workout_set_router.py#L40))
  - `available_equipment_router.bulk` ([:24](../serveur/app/routers/available_equipment_router.py#L24))
  - Refondre avec `list[XCreate]` (qui ne contient pas user_id). ✅ commits `d22031a` (actual_workout) + `4004539` (actual_workout_set) + `79191cc` (available_equipment) (2026-05-05)
- [x] **`upsert_planned_workout_exercise` cascade ownership manquante** ([planned_workout_exercise_crud.py:73-95](../serveur/app/crud/planned_workout_exercise_crud.py#L73)) — commentaire ligne 83 "il faudra vérifier via le planned_workout parent **si besoin**" non implémenté. Ajouter `await is_planned_workout_owned_by_user_uuid(...)`. ✅ commit `2041380` (2026-05-05, planned_workout + exercise cascade)
- [x] **`get_superset_exercise_by_uuid` reçoit `user_id` mais l'ignore** ([superset_exercise_crud.py:41-48](../serveur/app/crud/superset_exercise_crud.py#L41)) — JOIN commenté. Décommenter ou retirer le param. ✅ commit `f2cf3fa` (2026-05-05)
- [x] **`get_cycle_workout_by_uuid` sans check user_id** ([cycle_workout_crud.py:46-50](../serveur/app/crud/cycle_workout_crud.py#L46)) — utilisé par `delete_cycle_workout`. Lecture libre par UUID. ✅ commit `cfe7805` (2026-05-05)
- [x] **`upsert_cycle_workout` reçoit `user_id` mais ne l'utilise pas** ([cycle_workout_crud.py:53-80](../serveur/app/crud/cycle_workout_crud.py#L53)). ✅ commit `cfe7805` (2026-05-05, bundle avec get + model_dump)

### 🟠 Auth / DX (cohérence)

- [x] **Cascade ownership à auditer en pratique** sur les 5 entités feuilles (`actual_workout_set`, `actual_workout_exercise`, `planned_workout_exercise`, `routine_task_check`, `superset_exercise`). Vérifier que toute opération valide la chaîne d'ownership jusqu'à User. Stratégie acceptable : `user_id` dénormalisé OU JOIN cascade. **Interdit** : check uniquement sur le parent direct ou pas de check du tout. (Audit théorique fait en 2B-2 — à valider en pratique avec tests.) ✅ vague T1.1.c-bis (2026-05-06) — 5 tests pytest cross-user créés (1 par entité feuille), tous verts. **Bug bonus découvert + fixé** : `upsert_actual_workout_set_by_uuid` faisait UniqueViolation 500 au lieu de 403 sur uuid cross-user (le JOIN filtré user_id retournait rien → CREATE qui plante). Pattern aligné sur exercise_crud (2-step lookup global puis ownership). Cf. commit `82e84b5`.
- [x] **Ajouter `User.is_admin: bool`** + créer `Depends(require_admin)` dans `app/dependencies.py` — politique validée 2026-05-03. Migration : `ALTER TABLE users ADD COLUMN is_admin BOOLEAN NOT NULL DEFAULT FALSE` + `UPDATE users SET is_admin = TRUE WHERE username = 'will'`. ✅ commits `419e443` (modèle + schema + require_admin + /me) + `af27cbf` (migration Alembic + promote will) + `854b186` (Android Room v6→v7 + UserInfo) (2026-05-05)
- [x] **`get_current_user` retourne juste un username** ([auth.py:43](../serveur/app/auth.py#L43)) — chaque endpoint qui veut l'`user_id` doit re-query la DB. Modifier pour renvoyer un dict `{user_id, username}` ou un objet `User`. ✅ vague F1b (2026-05-06) — résolu de fait par V6.6-B (commit `f36b14e`) : `get_current_user_id` décode le JWT directement ; tous les endpoints user-scoped (~22 routers grep) utilisent `get_current_user_id`, plus aucun ne re-query la DB. Le seul callsite résiduel de `get_current_user` est `require_admin` qui a besoin de re-query par username pour lire `is_admin` (refactor en dict-retour ne sauverait pas ce SELECT). Pas de code change.
- [x] **`get_current_user_id` fait +1 SELECT par requête authentifiée** ([dependencies.py:9](../serveur/app/dependencies.py#L9)) alors que le JWT contient déjà `user_id`. Patch : décoder le token et lire `payload["user_id"]` directement, fallback DB seulement si absent. ✅ V6.6-B (commit `f36b14e`, 2026-05-05) — décode JWT directement, économie ~22 endpoints user-scoped.

### 🟡 Auth / robustesse

- [x] **`ws_router` peut crasher 500** si JWT sans `user_id` ([ws_router.py:20](../serveur/app/routers/ws_router.py#L20)) — `int(payload.get("user_id"))` raise `TypeError` si `None`. Ajouter `if "user_id" not in payload: await websocket.close(code=1008); return`. ✅ vague F1a (2026-05-06) — restructure try/except : `verify_token` dans son try `HTTPException`, puis check explicite `user_id_raw is None → close 1008`, puis `int(user_id_raw)` dans son try `(TypeError, ValueError) → close 1008`. Plus aucun chemin vers crash 500.

---

## §2 — Stabilité production

### 🔴 Stabilité

- [x] **Room sans `Migration` enregistrée** — `AppDatabase.kt` a `version = 6` et `AppModule.kt:51` `fallbackToDestructiveMigration(false)`. Aucune `Migration` enregistrée. **Tout futur bump de schéma fait crasher l'app pour les utilisateurs existants**. Mettre en place un système de migrations Room (cf. TODO_FEATURES). C'est un **bloqueur** pour les autres TODO qui touchent au schéma Room (par ex. suppression `MuscleWeeklySummary`, renommage colonnes camelCase, etc.). ✅ commits `2439fd1` (système de migrations + exportSchema=true + schémas v6/v7) + `854b186` (premier MIGRATION_6_7 réel pour `is_admin`) (2026-05-05). Reste à faire : doc DEV_GUIDE.md "Comment ajouter une migration Room" + tests `assertDatabaseHaveBeenMigratedTo`.
- [x] **`uvicorn --reload` actif en prod** ([start_api.sh:7](../serveur/start_api.sh#L7)) — `--reload` watch fichiers et redémarre, dangereux en prod (tous les WS perdus à chaque hit FS, leak fd potentiel). Désactiver pour le service systemd Pi. ✅ commit `7f2f8e7` (2026-05-04 QW)
- [x] **`exec_pg.py` est destructif** — drop+recreate de toutes les tables. À couper en 2 (cf. TODO_FEATURES) : `setup_db.py` idempotent + `run_server.py`. ✅ vague F9-Q5 (2026-05-06) — `exec_pg.py` scindé en 2 scripts à concerns séparés : (1) `setup_db.py` idempotent (`create_all` checkfirst + `CREATE OR REPLACE` helpers + `DROP IF EXISTS + CREATE` triggers), safe à relancer ; (2) `reset_db.py` destructif explicite avec **prompt confirmation interactif** (taper `reset` pour confirmer, sinon abandon). Lancement uvicorn **découplé** des 2 (séparation des concerns) — relance manuelle après. La Pi prod n'exécute jamais ces scripts (déploiement = `alembic upgrade head` via `deploy.sh`). DEV_GUIDE.md §4.2/4.2bis/cheatsheet/troubleshooting/§9-TODO mis à jour ; CLAUDE.md politique 4 + politique 6 + politique 16 (F8-Q3) + Quick reference mis à jour ; `triggers_loader.py` docstring + `20250811_notify_triggers.py` docstring re-référencés sur les nouveaux scripts. Smoke `python setup_db.py` confirmé idempotent.

---

## §3 — Sync (REST + WebSocket + dates)

> Voir [docs/INTEGRATION.md](INTEGRATION.md) §5 pour les flux détaillés et [docs/DATES.md](DATES.md) pour les bugs date.

### 🔴 Endpoints cassés (appelés en pratique)

- [x] **`cycleWorkoutApi.upsert(cw)` (PUT sans uuid) appelé en pratique** ([CycleWorkoutSyncable.kt:31](../appli-android/app/src/main/java/com/example/sportapp/sync/syncables/CycleWorkoutSyncable.kt#L31), [RemoteDataUpserter.kt:70](../appli-android/app/src/main/java/com/example/sportapp/sync/RemoteDataUpserter.kt#L70)) — endpoint `PUT /cycle-workouts` sans uuid **n'existe pas** côté serveur. 405 garanti. **Sync individuel CycleWorkout cassé**. Solution : aligner sur `upsert(uuid, item)` du squelette canonique. (fusion 3A + 3B + 5) ✅ commit `dcf4d71` (2026-05-05).
- [x] **`cycleWorkoutApi.delete(item)` (DELETE avec body) appelé en pratique** ([CycleWorkoutSyncable.kt:39](../appli-android/app/src/main/java/com/example/sportapp/sync/syncables/CycleWorkoutSyncable.kt#L39)) — endpoint inexistant. 405. Delete individuel cassé. Aligner sur `delete(uuid)`. ✅ commit `dcf4d71` (2026-05-05, bundle avec upsert).
- [x] **`exerciseMuscleApi.upsert(exerciseUUID, muscleUUID, em)` (PUT par paire) appelé en pratique** ([ExerciseMuscleSyncable.kt:31](../appli-android/app/src/main/java/com/example/sportapp/sync/syncables/ExerciseMuscleSyncable.kt#L31), [RemoteDataUpserter.kt:106](../appli-android/app/src/main/java/com/example/sportapp/sync/RemoteDataUpserter.kt#L106)) — le serveur a `PUT /exercise-muscles/{uuid}` (un seul UUID), pas par paire. 404 garanti. Aligner sur `upsert(uuid, item)`. ✅ commit `6ab569b` (2026-05-05) — upsert + delete standardisés.
- [x] **`SupersetExerciseApi.getByUUID(uuid: String)` → serveur attend `int`** ([superset_exercise_router.py:21-30](../serveur/app/routers/superset_exercise_router.py#L21)) — type mismatch UUID String vs `int`. Plus, le CRUD `get_superset_exercise_by_id` n'existe pas → 500 si le parse passait. **Solution** : router serveur doit utiliser `{uuid}: str` + `get_superset_exercise_by_uuid`. ✅ commit `f36b957` (2026-05-04 QW)
- [x] **`PUT /superset-groups/{uuid}` signature sans param `uuid`** ([superset_group_router.py:50-57](../serveur/app/routers/superset_group_router.py#L50)) — FastAPI raise 422 sur tout PUT. ✅ commit `d187ee5` (2026-05-04 QW)
- [x] **`PUT /training-cycles/{uuid}` signature sans param `uuid`** ([training_cycle_router.py:29-35](../serveur/app/routers/training_cycle_router.py#L29)) — 422 sur tout PUT. ✅ commit `b8f11d2` (2026-05-04 QW)

### 🔴 Triggers WS / payload

- [x] **5 entités sans push WebSocket** : `training_cycles`, `notifications`, `routine_periods`, `routine_tasks`, `routine_task_checks`. Pour chacune : créer un `<table>_trigger.sql` au format moderne (fragment IF TG_TABLE_NAME...) + ajouter à `exec_pg.py:load_sql_parts()` + étendre `user_id_helper.sql` pour les 4 user-scoped. Pour `training_cycles` : remplacer le legacy par fragment moderne. Conséquence actuelle : changements invisibles en temps-réel sur les autres devices, sync REST seulement. ✅ V4.3 entière (commits `ca95a27` user_id_helper + `85a6be3` 4 nouveaux fragments + `7fa8c9e` migration training_cycles + `3dcd971` ajout au PER_TABLE_FRAGMENTS, 2026-05-05).
- [x] **`training_cycles_trigger.sql` format LEGACY isolé** — fonction `notify_training_cycles_change()` séparée + `pg_notify` direct, **non chargée** par `exec_pg.py:load_sql_parts()`. Aucun `CREATE TRIGGER ... EXECUTE FUNCTION notify_training_cycles_change()` dans aucun fichier chargé → fonction définie jamais appelée. Attaché par `attach_triggers.sql` au générique → pas de bloc IF dans `base_function.sql` → payload NULL → skip. **`training_cycles` est muet en push**. Solution : migrer vers format moderne (fragment). ✅ commit `7fa8c9e` (2026-05-05).
- [x] **`sessions_trigger.sql` à supprimer** — fichier sur table `sessions` qui n'existe plus depuis le rename. Si chargé, raise SQL error. Format LEGACY également. (fusion étape "Confirmé" + 1 + 4) ✅ commit `eb7f985` (2026-05-04 QW)
- [x] **3 triggers payloadent sans `userId` malgré disponibilité de `get_user_id_for`** :
  - [`exercise_equipment_trigger.sql`](../serveur/app/db_triggers/exercise_equipment_trigger.sql) — `get_user_id_for('exercise_equipment',...)` existe et n'est pas appelé
  - [`exercise_muscle_trigger.sql`](../serveur/app/db_triggers/exercise_muscle_trigger.sql) — idem
  - [`planned_workout_exercises_trigger.sql:4-6`](../serveur/app/db_triggers/planned_workout_exercises_trigger.sql#L4) — bloc `delete` sans `userId` (le bloc `update` l'a)
  
  Conséquence : `ws_hub.broadcast` voit `userId is None` → broadcast à TOUS → côté Android `*SyncHandler` insert orphelins dans Room d'autres users. (fusion 2C + 4) ✅ commit `78693d0` (2026-05-05).
- [x] **Divergence dangereuse Alembic ↔ `db_triggers/` sur `notify_row_change()`** — payload simple Alembic `{table, op, id, uuid, user_id, updated_at}` vs riche db_triggers/ `{type, payload, userId, originClientId}`. Le format simple **casse silencieusement le dispatch côté Android** (`json.getString("type")` raise mangée par try/catch). Selon ordre `exec_pg.py` vs `alembic upgrade`, la dernière `CREATE OR REPLACE FUNCTION` gagne. **Solution** : retirer la définition de `notify_row_change()` de la migration Alembic (garder seulement l'attache des triggers via DO $$). Cf. [alembic 20250811_notify_triggers.py](../serveur/app/alembic/versions/20250811_notify_triggers.py) vs [base_function.sql](../serveur/app/db_triggers/base_function.sql). (fusion 2C + 3A + 4) ✅ commit `4a4dbe3` (2026-05-05) — résolu via helper partagé `app/triggers_loader.py` (Alembic = source de vérité, charge les fragments `db_triggers/*.sql` via le même helper qu'`exec_pg.py`).

### 🔴 Format wire des dates

- [x] **`RemoteDataMerger.isRemoteNewer` comparaison lexicographique fausse** ([RemoteDataMerger.kt:591-595](../appli-android/app/src/main/java/com/example/sportapp/sync/RemoteDataMerger.kt#L591)) — 3 formats wire coexistent (`Z` Android, `+00:00` Pydantic, `+01:00` trigger Pi). Cas pathologique : `local = "2025-01-15T14:30:00.000Z"` (14:30 UTC) et `remote = "2025-01-15T15:00:00.000+01:00"` (14:00 UTC = ancien) → la comparaison string retourne `remote > local` → écrasement intempestif d'une version récente par une ancienne. **C'est exactement le bug "réécriture sur changement de données en sync"** mentionné par l'utilisateur. Solution : parser en `Instant` (qui normalise UTC absolu) avant comparaison + try/catch fallback sur format Postgres legacy. (fusion 3B + 4.5) ✅ commit `6848cdd` (2026-05-05) + V3.2 entière (cf. ci-dessous).
- [x] **6 fonctions `CustomDateUtils` + 2 callsites externes crashent sur `Instant.parse` du format Postgres legacy avec espace** :
  - [CustomDateUtils.kt:48,54,60,67,74,183](../appli-android/app/src/main/java/com/example/sportapp/utils/CustomDateUtils.kt) (`fromISOToLocalDate`, `minusDays`, `startOfWeek`, `startOfMonth`, `startOfYear`, `formatRelativeTime`)
  - [DrawerViewModel.kt:92](../appli-android/app/src/main/java/com/example/sportapp/ui/components/drawer/DrawerViewModel.kt#L92)
  - [ExerciseStatsSection.kt:170](../appli-android/app/src/main/java/com/example/sportapp/ui/components/exerciseScreen/ExerciseStatsSection.kt#L170)
  
  Le commentaire de `toLocalDateFromDb:205-211` admet que ce format arrive en pratique. **Cause des crashs côté appli**. Solution : créer `parseInstantSafe(iso): Instant?` avec les 3 fallbacks (Instant, OffsetDateTime "yyyy-MM-dd HH:mm:ssXXX/X", LocalDate take(10)) puis remplacer tous les `Instant.parse` par cet helper. ✅ commit `6b67114` (2026-05-05).
- [x] **`ActualWorkoutSetDao.getDoneSetsForMuscleInWeek` utilise `date(aw.date)` SQLite** ([ActualWorkoutSetDao.kt:55](../appli-android/app/src/main/java/com/example/sportapp/data/local/ActualWorkoutSetDao.kt#L55)) — la fonction SQLite `date()` ne reconnaît PAS le format ISO complet (`T...Z`) → retourne NULL → query rate tous les sets. Bug latent : marche aujourd'hui car `date` contient `"YYYY-MM-DD"` strict, casse au premier write avec format datetime. Solution : remplacer par `substr(aw.date, 1, 10) BETWEEN ...`. ✅ vague F1c (2026-05-06) — substitué `date(aw.date)` → `substr(aw.date, 1, 10)`. Marche pour `"YYYY-MM-DD"` strict (cas actuel) ET `"YYYY-MM-DDTHH:..."` (cas latent). Params `startOfWeek`/`endOfWeek` confirmés au format `"yyyy-MM-dd"` via `CustomDateUtils.getStartOfWeek/getEndOfWeek`. Pas d'autre occurrence de `date(...)` SQLite dans `data/local/`.

### 🟠 Sync montante / descendante (architecture)

- [x] **Phase 1 (deletes) `SyncManager.syncAllToServer` dans le mauvais ordre FK** ([SyncManager.kt:189-201](../appli-android/app/src/main/java/com/example/sportapp/sync/SyncManager.kt#L189)) — l'ordre des `syncables` met les parents avant les enfants. Pour les deletes, l'ordre INVERSE est nécessaire. Bug masqué par `ON DELETE CASCADE` Postgres (vérifié en 4 : 28 FK CASCADE déclarées) qui supprime les enfants automatiquement → l'app envoie ensuite des DELETE sur des enfants déjà supprimés → 404 (géré par `syncEntityDeletions`). Pas crash, mais appels inutiles. Solution : `syncables.reversed().forEach { syncEntityDeletions(it) }`. ✅ commit `aa92f19` (V4.4-B1, 2026-05-05).
- [x] **`*SyncHandler` (WebSocket) n'ignore pas les payloads cross-user pour les junctions** — `WebSocketManager.handleMessage` dispatch sans filtrer ; `ExerciseMuscleSyncHandler.handle` insert le payload directement. Combiné avec les 3 triggers sans `userId` (cf. ci-dessus), un user A reçoit les modifs de B → orphelins en Room locale (FK pas validée par Room en insert simple). Solution : check `if (dao.getXByUUID(parentUUID) != null) insert; else ignore` dans handlers de junctions. ✅ commit `d2c20f2` (V4.4-C, 2026-05-05) — défense in depth ; le bug racine est résolu côté serveur via V4.2 (userId top-level).
- [x] **`mergeAllFromServer → syncAllToServer` ordre inversé** ([NetworkMonitor.kt:51-55](../appli-android/app/src/main/java/com/example/sportapp/utils/NetworkMonitor.kt#L51)) — le merge écrase un éventuel `pendingDeletion` local avant que celui-ci ait pu être push au serveur. Solution : push d'abord, merge ensuite. ✅ commit `0556bd1` (V4.4-B2, 2026-05-05).
- [x] **`mergeAllFromServer` non déclenché explicitement après login** ([AuthManager.kt](../appli-android/app/src/main/java/com/example/sportapp/auth/AuthManager.kt)) — dépend de `NetworkMonitor.onAvailable` qui peut ne pas se déclencher si le réseau était stable. Conséquence : après login sur un nouveau device, l'app affiche les données Room locales (vides ou obsolètes). Solution : appeler explicitement `remoteDataMerger.mergeAllFromServer()` à la fin de `AuthManager.initAuth()`. ✅ commit `4b83726` (V4.4-B3, 2026-05-05).
- [x] **`RemoteDataMerger` ne supprime pas les locaux absents du remote** — un device B garde un item supprimé sur device A indéfiniment, sauf reception du `*_deleted` WS qui peut être ratée (offline pendant push de A). Algorithme actuel ignore les locaux absents du remote. Solutions : (a) `merge.deleteLocalsAbsentFromRemote()` avec garde "ne delete que les `synced=true` absents" ; (b) implémenter un vrai soft-delete côté serveur (cf. §4 `deleted_at`). ✅ V4.4-différé (commit `c097977`, 2026-05-05) — helper `pruneStaleLocals(remote, local, keyOf, syncedOf, delete)` appelé après chaque merge entité (20×), filtre `synced=true` (protège créations locales). Refactor extrait suite à "Method too large" 65535 bytes JVM.
- [x] **`RemoteDataUpserter.upsertAllUnsynced` mélange `getAllUnsynced` et `getAllOnce`** ([RemoteDataUpserter.kt:35-243](../appli-android/app/src/main/java/com/example/sportapp/sync/RemoteDataUpserter.kt#L35)) — 6 entités (`availableEquipment`, `equipment`, `exerciseEquipment`, `exerciseMuscle`, `muscle`, `muscleGoal`) envoient TOUTES leurs données à chaque sync. Uniformiser sur `getAllUnsynced`. ✅ commit `b1e452c` (V4.4-D, 2026-05-05).
- [x] **`RemoteDataUpserter` incohérence User** : `upsertAllUnsynced` push les users (`userApi.upsert(u.id, u)`), `upsertAllData` a le bloc User commenté "TODO : User is not synced for security reasons". Trancher : politique sécurité validée → users non-modifiables sans admin, donc pas push depuis client. ✅ vague F8-A1 (2026-05-06) — bloc `users.forEach { userApi.upsert(...) }` retiré de `upsertAllUnsynced` (9 lignes), remplacé par un commentaire qui pointe la politique sécurité (CLAUDE.md §8). Cohérent avec le bloc déjà commenté de `upsertAllData`. `userDao` reste dans le constructeur (référencé par le bloc commenté `upsertAllData` + injecté par Hilt).
- [x] **`SyncManager.isSyncing: Boolean` simple** au lieu d'une `Mutex` — un crash entre `isSyncing = true` et le `finally` laisse l'app en état "sync bloquée" jusqu'au redémarrage. Remplacer par `Mutex.withLock`. ✅ commit `5f45926` (V4.4-A3, 2026-05-05).
- [x] **`payload.optString(key, null)` partout dans les `*SyncHandler`** — `JSONObject.optString` peut retourner la string `"null"` au lieu d'un `null` Kotlin. Utiliser `JsonUtils.getNullableString` systématiquement. ✅ commit `3790c85` (V4.4-A2, 2026-05-05) — sweep 10 occurrences sur 5 fichiers.
- [x] **`ExerciseSyncHandler.handle` n'extrait pas `instructions`** ([ExerciseSyncHandler.kt:22-38](../appli-android/app/src/main/java/com/example/sportapp/data/remote/ExerciseSyncHandler.kt#L22)) — le model `Exercise` a `instructions: List<String>?` et le trigger SQL l'envoie ([exercises_trigger.sql:17](../serveur/app/db_triggers/exercises_trigger.sql#L17)) mais le handler le perd. ✅ commit `3790c85` (V4.4-A2, 2026-05-05, bundle).
- [x] **`WebSocketManager.handleMessage` sans `else` dans `when`** ([WebSocketManager.kt:111-130](../appli-android/app/src/main/java/com/example/sportapp/data/remote/WebSocketManager.kt#L111)) — types inconnus silencieusement ignorés. Ajouter `else -> Log.w("WebSocket", "⚠️ Type inconnu : $type")`. ✅ commit `5ed4643` (V4.4-A1, 2026-05-05).
- [x] **`WebSocketManager.start` ne ferme pas l'ancien WS** ([WebSocketManager.kt:71](../appli-android/app/src/main/java/com/example/sportapp/data/remote/WebSocketManager.kt#L71)) — `webSocket = client.newWebSocket(...)` écrase la ref sans `webSocket?.close(...)`. Connexion fuit jusqu'à GC. ✅ commit `5ed4643` (V4.4-A1, 2026-05-05, bundle).
- [x] **Pas de gestion 401 proactive côté Android** — JWT expire (30 min) → `HttpException(401)` mangée dans `syncEntity` → snackbar "Sync error" trompeuse → futurs appels échouent en boucle. Solution : `Authenticator` OkHttp ou Interceptor 401 → `TokenManager.clearToken() + navigate to login`. ✅ V4.5 entière (commits `0e8d592` + `44cefe3` + `d349f0d`, 2026-05-05) — `Authenticator` OkHttp branché sur `authenticatedClient`, `SyncEvents.onTokenExpired` event, `MainActivity` hook nav login + snackbar "Session expirée", skip snackbar redondant dans `safeSync*WithSnackbar`. Refresh token (V8.2) prêt à être inséré au point d'extension documenté.
- [x] **Serveur `last-PUSH-wins` au lieu de `last-WRITE-wins`** — Avant fix : `upsert_X` écrasait aveuglément `existing` avec le payload sans regarder `updated_at`. Si 2 devices modifient la même row hors-ligne, le dernier à push ecrasait l'autre même s'il avait une version plus ancienne (lost update). **Asymétrie** : côté client, `mergeFromRemote` faisait déjà du last-write-wins (`isRemoteNewer` compare `updated_at` en `Instant`). Côté serveur, rien. Fix : helper centralisé `serveur/app/crud/_concurrency.py:is_payload_stale(payload_updated_at, existing_updated_at)` (~10 lignes) appliqué uniformément dans les 20 single-upserts (UserCrud exclu read-only F8-Q1) — 1 ligne d'import + 3 lignes `if is_payload_stale(...): return existing` avant le `for key, value in dto.model_dump().items()`. Convention NULL : si l'un des 2 `updated_at` est `None` (rows orphelines, payload legacy), on autorise l'écrasement (= comportement pré-fix). 3 nouveaux pytest dans `tests/test_concurrency.py` couvrent : older→skip, newer→overwrite, None→fallback overwrite. 16 pytest verts (13 existants + 3 nouveaux). Bulk-upserts pas couverts (extension différée, cf. [TODO_FEATURES.md §9](TODO_FEATURES.md)). ✅ session 2026-05-07.
- [x] **`insertFromServer` DAO ne pose pas `synced=true`** — Tous les 20 DAOs ([MuscleDao.kt:81-83](../appli-android/app/src/main/java/com/example/sportapp/data/local/MuscleDao.kt#L81), etc.) appelaient `insertInternal(item)` sans `.copy(synced=true)`. À la désérialisation JSON serveur (sans champ `synced`), le default Kotlin `false` du data class était posé en base. Après un `mergeFromRemote` (au login, à la reconnection WS, etc.), toutes les rows fraîchement reçues étaient `synced=false` → `pushAll` suivant les renvoyait au serveur inutilement (idempotent côté serveur mais pollution réseau + drawer affichait `unsynced=N` immédiatement après merge, ce qui faussait l'indicateur "j'ai du local à pousser"). Bug introduit par T4.2 (le commentaire `SyncableEntity.insertFromServer` disait "sans toucher aux flags synced/pendingDeletion" — exactement le bug). Solution : `insertInternal(item.copy(synced=true, pendingDeletion=false))` dans les 20 DAOs + même chose pour `insertAllFromServer` (`.map { it.copy(...) }`) + MAJ commentaire interface. Test régression `SyncMergeOpsTest.insertFromServer forces synced=true and pendingDeletion=false` ajouté (10 tests SyncMergeOps + 33 DAO smoke verts). UserDao exclu (read-only F8-Q1 = pas de flags `synced`/`pendingDeletion`). Auto-résolu post-fix (les rows existantes en `synced=false` se résorbent au prochain `pushAll` qui appelle `markAsSynced`). ✅ session 2026-05-07.

### 🟠 Dates (suite)

- [x] **3 formats wire pour le même `updatedAt`** — Android pose `Z` (UTC, jusqu'à 9 décimales), Pydantic REST renvoie `+00:00` (6 décimales), trigger Postgres renvoie `+01:00` (timezone Pi). **Cause racine de tous les bugs date**. Solution :
  - **Postgres trigger** : remplacer `'updatedAt', rec.updated_at` par `'updatedAt', to_char(rec.updated_at AT TIME ZONE 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS.US"Z"')` dans les 17 triggers
  - **Pydantic** : ajouter `field_serializer` qui force `Z`
  - **Android** : tronquer à microsec dans `getNowISO8601()` : `Instant.now().truncatedTo(ChronoUnit.MICROS).toString()` pour aligner précision
  ✅ V3.2 entière (commits `5b6cdd8` + `6d79827` + `bb6bae9` + `0cab7a9` + `6b67114` + `6848cdd`, 2026-05-05). Format wire canonique projet : `"YYYY-MM-DDTHH:MM:SS.UUUUUUZ"` imposé par 3 mécanismes (`iso_utc()` SQL / `UTCDateTime` Pydantic / `getNowISO8601()` Android). Cf. [docs/DATES.md §"Après V3.2"](DATES.md).
- [x] **Truncation silencieuse précision Android (9 → 6 décimales)** par Pydantic — chaque round-trip change l'`updatedAt` de 3 décimales. Avec `isRemoteNewer` lex, faux positifs constants. ✅ V3.2 (commit `0cab7a9` côté Android tronque à microsec ; commit `bb6bae9` côté Pydantic émet 6 décimales fixes).
- [x] **Variabilité précision `Instant.now().toString()`** — selon `nano = 0` ou non, la string fait 20/24/27/30 caractères. ✅ V3.2 (commit `0cab7a9` — formatter canonique force 6 décimales fixes via pattern `'.SSSSSS'Z'`).
- [x] **`ActualWorkoutDao.getActualWorkoutByDate(date)`** ([ActualWorkoutDao.kt:88](../appli-android/app/src/main/java/com/example/sportapp/data/local/ActualWorkoutDao.kt#L88)) `WHERE date = :date` — comparaison stricte. Ne match pas si `date` contient `"2025-01-15T..."` et on cherche `"2025-01-15"`. Solution : `substr(date, 1, 10) = :day` ou supprimer (doublon avec `getActualWorkoutByDay:92`). ✅ vague F4a-1 (2026-05-06) — option suppress retenue : `getActualWorkoutByDay` est déjà la version robuste (`substr`) avec 3 callsites (Calendar×2, Home×1). 1 callsite buggé (`SessionTabViewModel.getTodaySessionUUID:366`) migré vers `getActualWorkoutByDay`. Méthode buggée supprimée. Format de `today` confirmé `"yyyy-MM-dd"` strict via `CustomDateUtils.getTodayIsoDay()` → migration safe.
- [x] **7 fichiers utilisent `Instant.now()` / `LocalDate.now()` directement** au lieu de `CustomDateUtils` ([CalendarViewModel.kt:126,163](../appli-android/app/src/main/java/com/example/sportapp/viewmodel/CalendarViewModel.kt), [CalendarViewScreen.kt:49](../appli-android/app/src/main/java/com/example/sportapp/ui/screens/CalendarViewScreen.kt), [DrawerViewModel.kt:92-93](../appli-android/app/src/main/java/com/example/sportapp/ui/components/drawer/DrawerViewModel.kt#L92), [ExerciseStatsSection.kt:170,191](../appli-android/app/src/main/java/com/example/sportapp/ui/components/exerciseScreen/ExerciseStatsSection.kt)). Centraliser dans `CustomDateUtils`. ✅ vague F4b (2026-05-06) — 5 callsites prod hors `CustomDateUtils` migrés (`ExerciseStatsSection:170` était déjà `parseInstantSafe` V3.2). Helper `CustomDateUtils.getTodayLocalDate(zone: ZoneId): LocalDate` ajouté en overload. Migrations : `CalendarViewModel:112,148`, `CalendarViewScreen:50` → `getTodayLocalDate(zone)` ; `DrawerViewModel:94`, `ExerciseStatsSection:191` → `getNowInstant()`. Grep final confirme : 0 callsite `LocalDate.now()`/`Instant.now()` direct hors `CustomDateUtils.kt`. Source de vérité unique pour les dates côté Android.
- [x] **`DrawerViewModel.kt:92` et `ExerciseStatsSection.kt:170` `Instant.parse` sans try/catch** — wrapper avec `runCatching` ou `parseInstantSafe`. ✅ commit `6b67114` (2026-05-05) — les 2 utilisent maintenant `parseInstantSafe`.
- [x] **Pas de validation format Pydantic sur `date: str`** (catégorie B — `actual_workouts.date`, `routine_task_checks.date`) — un client peut envoyer datetime ISO complet → casse les queries SQLite côté autre device. Solution : `Field(..., pattern=r"^\d{4}-\d{2}-\d{2}$")`. ✅ vague F4a-2 (2026-05-06) — pattern strict ajouté dans `actual_workout_schema.ActualWorkoutBase.date` et `routine_task_check_schema.RoutineTaskCheckBase.date`. Smoke E2E : accepte `"2026-05-06"`, rejette `"2026-05-06T14:30:00Z"` (422 ValidationError) et `"not-a-date"`. 3e source de vérité pour le format wire dates ajoutée (avec `iso_utc()` SQL et `getNowISO8601()` Android = format unifié projet).

### 🟡 Sync (mineur)

- [x] **`RemoteDataGetter.getAll` pose `synced = false` sur des données serveur** ([RemoteDataGetter.kt:46-203](../appli-android/app/src/main/java/com/example/sportapp/sync/RemoteDataGetter.kt#L46)) — bug logique : juste après un `getAll()`, l'app pense que toutes ses données sont à push. Le code est probablement mort (jamais appelé) — confirmer par grep et supprimer si oui. ✅ vague F1+C (2026-05-06) — finding requalifié : pas mort (1 callsite confirmé `SyncSettingsViewModel.getAllTablesFromServer:202`, exposé via bouton dev settings) et `synced=false` était **intentionnel** (workflow utilisateur : pull serveur en marquant unsynced, puis test push). Solution : split en 2 méthodes — `getAll(synced=true)` prod-style cohérent avec source serveur (pas encore appelé) + `getAllAsUnsynced(synced=false)` dev tool documenté KDoc "ne pas utiliser en prod, double-push silencieux". Helper privé `fetchAllAndPersist(syncedAfter, log)` partagé (zéro duplication). Bug latent en passant : `var log = true` ligne 35 shadow le param `log: Boolean = false` (forçait toujours `log=true`) → éliminé naturellement par le helper paramétré. Callsite migré vers `getAllAsUnsynced(log = true)` (préserve workflow dev existant).
- [x] **`syncEntity` pas de retry exponentiel** — items unsynced restent en attente jusqu'à la prochaine sync. Acceptable. ✅ vague F4a-4 (2026-05-06) — décision écrite : pas de retry exponentiel nécessaire car (a) `WorkManager` Android relance la sync à intervalles réguliers, (b) `NetworkMonitor.onAvailable` déclenche un push immédiat à la reconnexion, (c) pas de risque de perte (les unsynced restent locaux). Pas de fix.
- [x] **`RemoteDataMerger` bloc Users commenté + `isLocalNewer` jamais utilisé** — bloc Users intentionnel (sécurité OK), mais `isLocalNewer` est code mort. ✅ V7.4-C2 (commit `a5c5846`, 2026-05-05) — `isLocalNewer` retiré.
- [x] **`RemoteDataMerger.mergeAllFromServer` shadow var** ([RemoteDataMerger.kt:37](../appli-android/app/src/main/java/com/example/sportapp/sync/RemoteDataMerger.kt#L37)) — `val log = true` écrase le param `log: Boolean = false`. Idem `RemoteDataGetter.kt:35`. ✅ V7.4-C2 (commit `a5c5846`, 2026-05-05) — shadow var retirée + param par défaut bumpé à `true`.
- [x] **`UserSyncable` n'est pas dans `SyncManager.syncAllToServer`** — décision intentionnelle (sécurité) mais à documenter par un commentaire de code. ✅ vague F4a-3 (2026-05-06) — commentaire ajouté avant la liste `syncables` dans `SyncManager.kt` qui explique la politique sécurité (CLAUDE.md §8) et le fait que la classe `UserSyncable` reste disponible pour usage admin futur. Variable orpheline `val userSync = UserSyncable(userDao)` retirée + import (warnings Kotlin éliminés).

---

## §4 — Schéma DB (Postgres + Pydantic + Room)

> Voir [docs/DATABASES.md](DATABASES.md) pour le détail (22 fiches comparatives Postgres / Pydantic / Room).

### 🔴 Mismatches Postgres/Pydantic/Room

- [x] ~~**`SupersetGroup.userId: Int? = null` nullable côté Room** mais `user_id: int` non-nullable côté Pydantic — push avec `userId=null` → 422.~~ ✅ commits `949ad05` + `444bc2e` + `736fe13` (2026-05-05, V5.2-C). MIGRATION_7_8 copy-table avec backfill `user_id=1` (politique CLAUDE.md §12). SyncHandler durci option (b). V2.2 préreq déjà appliqué (commit `c1981f9`).
- [x] ~~**`SupersetExercise.orderInGroup: Int? = null` nullable côté Room** mais `order_in_group: int` obligatoire côté Pydantic. Rendre non-nullable côté Room avec default `0`.~~ ✅ idem V5.2-C. Backfill `0`. SyncHandler durci option (b).
- [x] ~~**`actual_workout_sets.target_reps` champ Room fantôme** — `targetReps: Int = 10` côté Room, sans équivalent Postgres ni Pydantic ni trigger. Décider (a)/(b).~~ ✅ Décision **option (b) retirer Room+VMs** (sous-vague E, 2026-05-05) appliquée en sous-vague F : commit `e5e3624` retire le champ du modèle Room ; commit `9efe3b3` retire `targetReps = target` + `parseTargetReps` locale dans HomeViewModel + CalendarViewModel. Analyse confirmée : le champ était écrit (Home/Calendar VMs au création des sets depuis planned_workout) mais **jamais lu** côté UI — `targetRepsRange` est en fait dérivé de `actualWorkoutExercise.reps` (string `"8-12"` parsée par `parseRepsRange`).

### 🟠 Modèle Room — colonnes camelCase incohérentes (4 cas)

- [x] ~~**`ActualWorkout.userId` SANS `@ColumnInfo(name = "user_id")`** — la colonne SQLite réelle est `userId` (camelCase). Toute future query `WHERE user_id = ?` cassera.~~ ✅ commit `949ad05` (2026-05-05, V5.3-C). MIGRATION_7_8 `ALTER TABLE actual_workouts RENAME COLUMN userId TO user_id`.
- [x] ~~**`Muscle.isFavorite` SANS `@ColumnInfo`** — col `isFavorite` au lieu de `is_favorite`.~~ Découvert lors de C : déjà OK (`@ColumnInfo("is_favorite")` déjà présent à la ligne [Muscle.kt:20](../appli-android/app/src/main/java/com/example/sportapp/data/model/Muscle.kt#L20)). Item fermé sans modif (probablement déjà fixé hors audit).
- [x] ~~**`MuscleGoal` 5 colonnes camelCase** — `priority`, `done`, `target`, `weekISO`, `status` sans `@ColumnInfo`.~~ Découvert lors de C : 4/5 sont des noms 1-mot (`priority`, `done`, `target`, `status`) sans souci de casse côté SQLite. Seul `weekISO` est vraiment camelCase, mais c'est un item 🟡 cosmétique séparé multi-stack (Postgres+Pydantic+Room) — voir item dédié ci-dessous "muscle_goals.weekISO colonne Postgres camelCase". Item V5.3 fermé sans modif.
- [x] ~~**`ActualWorkoutExercise.addedManually` SANS `@ColumnInfo`** — col `addedManually`.~~ ✅ commit `949ad05` (2026-05-05, V5.3-C). MIGRATION_7_8 `ALTER TABLE actual_workout_exercises RENAME COLUMN addedManually TO added_manually`.

### 🟠 Modèle SQLAlchemy — `nullable` manquants / incohérents

- [x] ~~**`muscle_weekly_summary.user_id` sans `nullable=False`**.~~ ✅ Caduc — entité supprimée V5.1-G (2026-05-05).
- [x] ~~**`muscle_weekly_summary.week_start_date` et `total_sets` sans `nullable=False`**.~~ ✅ Caduc — entité supprimée V5.1-G (2026-05-05).
- [x] ~~**`muscle_goals.uuid` sans `nullable=False`**.~~ ✅ commits `1dab40b` + `f89df59` (2026-05-05, V5.4-B).
- [x] ~~**`actual_workout_sets.is_dropset` sans `nullable=False`** — `Column(Boolean, default=False)` permet null.~~ ✅ commits `1dab40b` + `f89df59` (2026-05-05, V5.4-B) — `nullable=False, server_default="false"` (sémantique : set normal ~95%).
- [x] ~~**`exercise_muscles.coefficient` sans `nullable=False`** — Pydantic obligatoire.~~ ✅ commits `1dab40b` + `f89df59` (2026-05-05, V5.4-B) — `nullable=False, server_default="1"` (sémantique : muscle primaire à 100%, cf. CLAUDE.md politique 10).
- [x] ~~**`actual_workout_sets.status: Optional[str]` côté Pydantic** vs `nullable=False default="in_progress"` Postgres. Pydantic trop laxiste.~~ ✅ commit `8e55897` (2026-05-05, V5.4-B) — `Optional[str] = None` → `str = "in_progress"`. Correction sémantique (`NOT_STARTED`) + UPPER_CASE reportée en vague cosmétique.

### 🟠 Modèle SQLAlchemy — defaults manquants

- [x] ~~**`actual_workout_exercises.phase` sans default Postgres** — `nullable=False` sans default. INSERT serveur sans `phase` raise. Décider : ajouter `default="TRAINING"` (cohérent avec PlannedWorkoutExercise) ou imposer côté Pydantic.~~ ✅ commits `1dab40b` + `f89df59` (2026-05-05, V5.4-B) — `default="TRAINING", server_default="TRAINING"` (sémantique : exercice classique vs WARMUP/POST_TRAINING particuliers, cf. CLAUDE.md politique 10).
- [x] ~~**`actual_workout_sets.set_order` sans default Postgres** — `nullable=False` sans default. Room a `setOrder: Int = 0`.~~ Décision validée 2026-05-05 (V5.4) : **pas de default Postgres** — la position est positionnelle/métier, doit être explicite (cf. CLAUDE.md politique 10). Room garde son default pour la création locale, mais le serveur exigera la valeur. Item fermé sans modif serveur.
- [x] **`actual_workout_exercises.reps` Room `defaultValue = "0"`** ([ActualWorkoutExercise.kt:38](../appli-android/app/src/main/java/com/example/sportapp/data/model/ActualWorkoutExercise.kt#L38)) — pour String, default mal posé. Postgres a `default="0-1"`. ✅ vague F8-A2 (2026-05-06) — `defaultValue = "0"` → `"0-1"` côté Room. Migration `MIGRATION_12_13` copy-table (SQLite ne supporte pas ALTER COLUMN SET DEFAULT) : CREATE awe_new avec nouveau DEFAULT + INSERT SELECT (rows existants conservent leur reps actuelle) + DROP + RENAME + recréer index UNIQUE sur uuid + 2 FK CASCADE. Le DEFAULT '0-1' s'applique uniquement aux NOUVEAUX inserts sans valeur explicite (cas rare, les VMs posent depuis `recommended_reps` du parent).

### 🟠 Audit transverse — defaults par sémantique métier (validé 2026-05-05)

- [x] **Auditer tous les `default` / `server_default` existants** sur les 22 modèles SQLAlchemy + Pydantic + Room par rapport au critère [CLAUDE.md §10](../CLAUDE.md) : un default est légitime si la valeur reflète **une sémantique métier majoritaire** (cas naturel par défaut), pas si elle comble arbitrairement un trou. Lister les defaults arbitraires à retirer (force raise sur omission). Travail à faire après V5.4. ✅ Audit complété en V5.4-B (commit `1dab40b`) + F2c-1 (status NOT_STARTED) + F2c-2 (week_iso). Defaults sémantiques validés : `coefficient=1.0` (muscle primaire), `is_dropset=False` (set normal), `phase="TRAINING"` (exercice classique), `status="NOT_STARTED"` (set créé pas commencé), `status="IN_PROGRESS"` (goal en cours). `set_order` sans default (positionnel). Plus rien d'arbitraire identifié.
- [x] **Aligner tous les états en `UPPER_CASE` cross-stack** (cf. [CLAUDE.md §11](../CLAUDE.md)) — `actual_workout_sets.status` lowercase `"in_progress"` (vs `actual_workout_exercises.status` uppercase `"NOT_STARTED"`) ; vérifier également `priority`, et tout autre champ de type "état" côté serveur (Postgres + Pydantic) ET côté Android (Room + Kotlin). Migrer en bloc dans une vague cosmétique dédiée. **Bonus côté `actual_workout_sets.status`** : la valeur sémantique correcte par défaut est `"NOT_STARTED"` (et non `"in_progress"`) — un set créé est par défaut non commencé, sauf si on insère un set déjà fait après coup. ✅ vague F2c-1 (2026-05-06) — `actual_workout_sets.status` était le seul état lowercase. Fix complet : default Postgres + Pydantic + DBML alignés sur `"NOT_STARTED"` (UPPER_CASE + sémantique correcte). Migration Alembic `38449dc119b5` : `UPDATE actual_workout_sets SET status='NOT_STARTED' WHERE status='in_progress'` + `ALTER COLUMN ... SET DEFAULT 'NOT_STARTED'`. Audit Android confirmé : tous les `status` côté Kotlin déjà UPPER_CASE (`"NOT_STARTED"`, `"DONE"`, `"IN_PROGRESS"`, `"PLANNED"`, `"NEXT"`, `"SKIPPED"`, `"WARMUP"`). 0 row lowercase restant après migration.
- [x] **Auditer la cohérence des scripts de seed/fill ↔ modèles** (cf. [CLAUDE.md §13](../CLAUDE.md)) — passer en revue `app/seed_database.py` + `app/fill_database.py` et vérifier que **chaque INSERT respecte les champs obligatoires + contraintes + defaults** des modèles SQLAlchemy actuels. À refaire à chaque modification de schéma (point de checklist permanent). Premier passage à faire après V5.7 (TrainingCycle.user_id ajouté) et V5.4 (defaults sémantiques). ✅ vague F2c (2026-05-06) — `seed_database.py` et `fill_database.py` audités après V5.4 (defaults sémantiques)/V5.7 (TC.user_id)/F2c-2 (week_iso). 5 lignes `weekISO=` migrées vers `week_iso=`. Smoke `import app.seed_database` OK. Statuts seed (`status="IN_PROGRESS"`, `"NOT_STARTED"`, `"DONE"`, `"PLANNED"`) déjà UPPER_CASE. Politique : à refaire ponctuellement à chaque ajout de colonne obligatoire — checklist mentale au commit (cf. CLAUDE.md §13).

### 🟠 Soft-delete + helpers

- [x] ~~**`deleted_at` jamais posé par aucun CRUD serveur** — chemin aspirationnel jamais terminé. Décider (a) retirer / (b) implémenter vrai soft-delete.~~ ✅ **Option A retenue** (sous-vague E, 2026-05-05) appliquée en sous-vague F (8 commits) : `deleted_at` retiré des 21 tables Postgres (migration Alembic `20260505_v5_5_drop_deleted_at`), 21 modèles SQLAlchemy + 5 CRUDs, 21 schémas Pydantic, 21 fragments triggers SQL + diagram.dbml, 21 entités Room (migration `MIGRATION_8_9` v8→v9), 17 SyncHandlers, 21 branches `RemoteDataMerger`, NotificationRepository, VMs (Home/Calendar/Session/RoutineTasks) + MuscleListScreen. Politique deletes : `pendingDeletion=true` côté client → push serveur → hard DELETE → broadcast WS. *Item différé V4.4 `deleteLocalsAbsentFromRemote(synced=true)` reste à ajouter dans RemoteDataMerger pour fermer le cycle de sync delete.*
- [x] ~~**`user_id_helper.sql` manque 4 entités user-scoped** : `notifications`, `routine_periods`, `routine_tasks`, `routine_task_checks` (toutes user_id direct). Et **retirer le cas `sessions`** (vestige).~~ ✅ V4.3 (2026-05-05, commit `ca95a27`).
- [x] ~~**`training_cycles` `get_user_id_for` JOIN fragile** — retourne le user_id du **premier** workout joint. Si plusieurs users (théoriquement impossible mais structurellement possible), seul le 1er reçoit.~~ ✅ **Type A retenu** (V5.7, 2026-05-05) — `training_cycles` a maintenant une colonne `user_id` directe (FK users.id ondelete CASCADE), helper simplifié en lookup direct, router/CRUD passés en user-scoped, ownership check via `Depends(get_current_user_id)` + 403 si cross-user. Migration Alembic `20260505_v5_7_tc_user_id` (backfill 5 cycles → user_id=1 via politique CLAUDE.md §12). Smoke test E2E confirmé.
- [x] **`User` Room sans `updatedAt`/`deletedAt`** ([User.kt](../appli-android/app/src/main/java/com/example/sportapp/data/model/User.kt)) — empêche tout 3-way merge / soft-delete propre. Cohérent avec politique sécurité (users non-syncés depuis client) mais à clarifier : retirer `synced`/`pendingDeletion` aussi (= read-only côté client) OU ajouter les timestamps. ✅ vague F8-Q1 (2026-05-06) — option (a) retenue : User read-only client pur. **Côté Room** : retire `synced` + `pendingDeletion` du modèle `User.kt`. **DAO** : `UserDao.kt` simplifié (retire `getAllUnsynced`/`markAsSynced`/`markAsUnsynced`/`hasUnsynced`/`markAsPendingDeletion`/`getPendingDeletions`/`*Internal` + wrappers `synced=false`). `UserSyncable.kt` supprimé (0 callsite externe). Callsites cleanup : `RemoteDataGetter:119` retire `.copy(synced=...)`, `SyncSettingsViewModel` retire bloc commenté `is User ->`, `SyncManager` commentaire mis à jour. Migration Room `MIGRATION_12_13` : `ALTER TABLE users DROP COLUMN synced` + idem `pendingDeletion` (pattern MIGRATION_8_9 — DROP COLUMN SQLite >= 3.35 / API 34+, accepté pour la base dev/perso).

### 🟠 Diagram DBML obsolète

- [x] **`diagram.dbml` indexes aspirationnels non implémentés** ([diagram.dbml:361-368](../serveur/app/diagram.dbml#L361)) — 5 indexes annoncés sur `notifications` + indexes sur `routine_periods/tasks/task_checks`. Décider : (a) implémenter dans modèles SQLAlchemy (préférable perfo), (b) retirer du DBML. ✅ vague F2a (2026-05-06) — option mixte : (a) pour les indexes utiles immédiatement (`(user_id)` posé F4c, `(user_id, created_at)` posé F4d), (b) pour les vraiment aspirationnels (`(user_id, read_at)`, `(user_id, dedupe_key) [unique]`) retirés du DBML. Si besoin perf futur, on les ajoutera avec une vraie analyse mesurée.
- [x] **`diagram.dbml` types aspirationnels** :
  - `routine_periods.start_time/end_time` annotés `time` ([diagram.dbml:284-285](../serveur/app/diagram.dbml#L284)) — modèles utilisent `String`
  - `routine_task_checks.date` annoté `date` ([diagram.dbml:325](../serveur/app/diagram.dbml#L325)) — modèle utilise `String`
  ✅ vague F2a (2026-05-06) — DBML aligné sur l'effectif : `varchar` partout avec commentaire `String côté SQLAlchemy + Pydantic + Room` / `"YYYY-MM-DD" strict (validé Pydantic pattern, cf. F4a-2)`.
- [x] ~~**`diagram.dbml` FK `muscle_weekly_summary.user_id` sans `[delete: cascade]`**.~~ ✅ Caduc — entité supprimée V5.1-G (2026-05-05, commit `a461401` retire bloc Table + 2 Refs FK).
- [x] **Politique de traçabilité des changements** (mémoire 2026-05-04) — tout changement DB doit mettre à jour `diagram.dbml` + `diagram.dbdiagram`. Convention : DBML = source de l'**intention**, modèles SQLAlchemy = source de l'**état effectif**. Documenter. ✅ vague F6-6 (2026-05-06, commit `54f1ca1`) — politique formalisée dans `CLAUDE.md §14` : sources de vérité listées (DBML = intention, models SQLAlchemy = effectif, DATABASES.md = audit, SERVEUR.md §3 = inventaire types, CLAUDE.md historique = chronologie). Convention "même commit pour code+doc" + Room schemas JSON auto-générés à committer. Item ticker en F11-cleanup (2026-05-06).

### 🟠 Path API + naming

- [x] **Path `/exercise-equipment` (singulier) vs autres pluriels** — incohérence convention REST. Renommer en `/exercise-equipments` partout (router serveur + API Android + diagram + tests). ⚠ Migration multi-stack. ✅ vague F2b (2026-05-06) — 5 paths router serveur + 5 paths Retrofit Android renommés en `/exercise-equipments`. Smoke `app.routes` confirme les 5 nouvelles routes pluriel. **Atomicité multi-stack** : nécessite déploiement coordonné serveur + Android (l'app avec le nouveau path ne marchera pas tant que le serveur ne renvoie pas, et inversement). Pour la transition : déployer serveur d'abord (Pi prod), puis le build Android.

### 🟡 Schéma DB — anomalies cosmétiques

- [x] **`muscle_goals.weekISO` colonne Postgres camelCase** ([muscle_goal.py:17](../serveur/app/models/muscle_goal.py#L17)) — anomalie symétrique avec Pydantic (sans alias) et Room (sans `@ColumnInfo`). Refactor : renommer `weekISO → week_iso` partout + alias `weekISO` côté Pydantic. Migration multi-stack. ✅ vague F2c-2 (2026-05-06) — rename complet en 6 fichiers + 2 migrations : (a) modèle SQLAlchemy `weekISO` → `week_iso` ; (b) migration Alembic `f2c2_weekiso` (`ALTER TABLE muscle_goals RENAME COLUMN "weekISO" TO week_iso`) appliquée PC dev ; (c) Pydantic `MuscleGoalBase.week_iso: str = Field(..., alias="weekISO")` (préserve wire JSON `weekISO`) ; (d) trigger SQL `muscle_goals_trigger.sql` payload `'weekISO', rec.week_iso` (col Postgres change, payload garde `weekISO`) → reload via `triggers_loader` ; (e) `seed_database.py` 5 lignes `weekISO=iso_week` → `week_iso=iso_week` ; (f) Room `MuscleGoal.kt` ajout `@ColumnInfo(name = "week_iso")` (Kotlin field reste `weekISO`) ; (g) `MuscleGoalDao.kt` 2 queries `WHERE weekISO` → `WHERE week_iso` ; (h) Migration Room `MIGRATION_11_12` (`ALTER TABLE muscle_goals RENAME COLUMN weekISO TO week_iso`, SQLite ≥3.25 / API 30+) ; (i) `diagram.dbml` mis à jour. Smoke E2E : `MuscleGoalCreate(weekISO=...).week_iso` lit OK + `model_dump_json(by_alias=True)` émet `"weekISO"`. **Note Pi prod** : `alembic upgrade head` (rev `f2c2_weekiso`) + recharger triggers via `psql -f` (commande dans `triggers_loader`).
- [x] ~~**`muscle_weekly_summary` table singulière** — toutes les autres au pluriel.~~ ✅ Item caduc — table supprimée intégralement en sous-vague G (V5.1, 2026-05-05).
- [x] **Conventions de status incohérentes** — `actual_workout_sets.status` default `"in_progress"` lowercase vs `actual_workout_exercises.status` default `"NOT_STARTED"` uppercase. **Double correction à faire en bloc** : (1) sémantique — `actual_workout_sets.status` default sémantiquement correct = `"NOT_STARTED"` (set créé = pas commencé) et non `"in_progress"` ; (2) casse — aligner tous les états sur `UPPER_CASE` cross-stack (cf. [CLAUDE.md §11](../CLAUDE.md)). Lié à l'item d'audit transverse §4 ci-dessus. ✅ vague F2c-1 (2026-05-06) — fix complet des 2 corrections en 1 vague. Cf. item résolu ci-dessus "Aligner tous les états en UPPER_CASE cross-stack".
- [x] **`uuid: Optional[str]` dans 4 schémas Pydantic** — `actual_workout_schema:17`, `actual_workout_exercise_schema`, `actual_workout_set_schema`. Devrait être `str` strict. Cf. [actual_workout_schema.py:17](../serveur/app/schemas/actual_workout_schema.py#L17). ✅ Résolu de fait par V6.3 (commit `c7a1b58`, 2026-05-05) — les 3 schémas `actual_workout*` ont déjà `uuid: str` strict. Item ticker en F2a (2026-05-06).
- [x] **`Notification.created_at: Optional[datetime]` côté Pydantic** vs `nullable=False server_default=func.now()` côté Postgres — Pydantic ne peut pas garantir non-null à la sortie. Aligner. ✅ vague F2a (2026-05-06) — override dans `NotificationOut` : `created_at: UTCDateTime = Field(..., alias="createdAt")` (non-Optional). `NotificationCreate` reste `Optional[UTCDateTime] = None` (le client peut omettre, le serveur défaultera via `func.now()`). Le typage Out reflète maintenant l'invariant Postgres NOT NULL.
- [x] **`Notification.createdAt: String? = null` côté Room non auto-rempli** ([Notification.kt:36](../appli-android/app/src/main/java/com/example/sportapp/data/model/Notification.kt#L36)) — convention fragile. Mettre `createdAt: String? = getNowISO8601()` par défaut (en pratique `NotificationRepository.build()` pose la valeur, donc pas de bug actif, mais piège latent). ✅ vague F2a (2026-05-06) — default Kotlin `getNowISO8601()` ajouté. Si jamais `NotificationRepository.build()` est court-circuité, le default Kotlin pose une valeur cohérente (format wire canonique projet).
- [x] **`routine_periods.order` Postgres vs `routine_periods.order_index` Room** — 2 noms de colonnes différents pour le même champ logique. Wire JSON utilise `order` (cohérent Pydantic). Soit renommer Postgres `order → order_index`, soit Room `order_index → order` (en gardant `@ColumnInfo("order_index")`). ✅ vague F7-1 (2026-05-06) — option "renommer Postgres" retenue (mot-clé SQL `order` non échappé = mauvaise pratique). Migration Alembic `f71_order_idx` : `ALTER TABLE routine_periods RENAME COLUMN "order" TO order_index` + idem `routine_tasks`. Modèles SQLAlchemy : `order = Column("order_index", Integer, ...)` (attribut Python `order` reste cohérent avec Pydantic + Room field, col Postgres devient `order_index` cohérente avec Room SQLite). Triggers SQL `routine_periods_trigger.sql` + `routine_tasks_trigger.sql` payload `'order', rec.order_index` (col change, payload garde `order`). Reload via `triggers_loader`. Seed `order=1,2,3` reste valide. **Note Pi prod** : `alembic upgrade head` (rev `f71_order_idx`) + recharger triggers via `psql -f`.
- [x] **`AvailableEquipment` Type C global** (Pydantic + SQLAlchemy + Room — tous sans `user_id`) **mais le NOM suggère user-scoped** ("MON équipement disponible"). À clarifier fonctionnellement : si user-scoped, ajouter `user_id` partout. Cohérent avec [§1 router public]. ✅ vague F8-Q2 (2026-05-06) — Type A user-scoped retenu (sémantique métier confirmée par utilisateur : "parmi les équipements existants, j'ai celui-ci"). Cascade complète serveur + Android. **Serveur** : modèle SQLAlchemy ajout `user_id` FK CASCADE + `UniqueConstraint(user_id, name)` + retrait `unique=True` sur name ; schéma Pydantic `Out` ajoute `userId` ; CRUD signatures user-scoped + ownership 403 + delete bool ; router retire `require_admin`, `Depends(get_current_user_id)` partout ; helper `user_id_helper.sql` ajout case `available_equipments` ; trigger fragment `userId` top-level + dans payload imbriqué ; migration Alembic `f8q2_ae_user_id` (ADD user_id + backfill user_id=1 via politique CLAUDE.md §12 + FK CASCADE + index + DROP unique(name) + ADD unique(user_id,name) + reload `notify_row_change()`) ; seed `seed_database.py` user_id=1 sur les 5 AE seeds ; DBML aligné (col + indexes + Ref FK) ; docs/SERVEUR.md §3 archétypes (12 Type A). **Android** : modèle Room ajout `userId: Int` + index ; `AvailableEquipmentSyncHandler.handle` lit `userId` du payload ; migration Room `MIGRATION_12_13` (ADD COLUMN user_id INTEGER NOT NULL DEFAULT 1 + index). Smoke E2E PC dev OK (5 routes available-equipments + UNIQUE constraints posées).
- [x] **`TrainingCycle.startDate/endDate: String` côté Room** vs `start_date: date / end_date: date` côté Pydantic — Pydantic parse `"YYYY-MM-DD"` correctement. Mais si client envoie datetime ISO complet, raise. Documenter format ou normaliser en String partout. ✅ vague F7-3 (2026-05-06) — comportement Pydantic v2 confirmé strict : `"2025-01-15"` OK, `"2025-01-15T14:30:00Z"` rejette en 422 (`date_from_datetime_inexact`). Pas de bug, juste doc manquante. Commentaire ajouté dans `TrainingCycleBase` qui explicite le format wire strict `"YYYY-MM-DD"` côté serveur ET côté Room (cohérent F4a-2 sur autres dates `date: str`).
- [x] ~~**`MuscleWeeklySummary.weekStartDate`**.~~ ✅ Caduc — entité supprimée V5.1-G (2026-05-05).
- [x] **`MuscleOut.id: int` exposé** — autre que `MuscleOut`, seul `User*` a `id`. Cohérent ? L'app Android utilise uniquement `uuid`. Le `id` est superflu. Confirmer. ✅ vague F7-2 (2026-05-06) — confirmé superflu : grep `muscle.id` côté serveur retourne 0 callsite (autre que `MuscleOut(MuscleBase)`), modèle Room `Muscle.kt` n'a pas de field `id` (seulement `uuid` PK). `id: int` retiré de `MuscleOut`. Cohérent : seules les entités `User*` exposent `id` (légitime, c'est la référence FK pour `user_id` ailleurs).

---

## §5 — Architecture / Squelette uniforme / Refactor

> Politique squelette uniforme par module type (mémoire 2026-05-03 + précision 2026-05-04 sur conservation des fonctions du squelette même non-utilisées).

### 🔴 Bugs Room / DAO architecturaux

- [x] ~~**`RoutineTaskCheckDao.setChecked` viole 2 couches** — un `@Dao` (data layer) `import CurrentUserManager` (network) ET `import showSnackbar` + `SnackbarType` (UI Compose).~~ ✅ commit `3660409` (2026-05-05, V6.1-D1) : DAO ne contient plus que des opérations CRUD pures, logique déplacée dans `RoutineTasksScreenViewModel.toggleTask` (seul appelant). Comportement utilisateur identique (snackbar erreur si userId null).
- [x] ~~**`ActualWorkoutSetDao` Style C : `insert/insertAll/updateActualWorkoutSet` annotés sans body** — ne posent pas `synced=false` ni `updatedAt`.~~ ✅ commit `8cd25d7` (2026-05-05, V6.1-D2) : Style A canonique appliqué — wrappers `insert/insertAll/update` posent automatiquement `synced=false + updatedAt`, délèguent à `*Internal`. `*FromServer` préservent payload (synced=true). `updateActualWorkoutSet` renommé `update` (0 callsite à migrer). Build compileDebugKotlin OK.
- [x] ~~**`CycleWorkoutDao` bug Room `@Query` + body Kotlin** — `markAsPendingDeletion(uuid)` annoté `@Query("UPDATE ... SET pendingDeletion = 1 WHERE uuid = :uuid")` (sans toucher `updated_at`) avec body Kotlin qui appellerait `markAsPendingDeletionInternal(uuid, now)` (qui touche `updated_at`). **Room génère depuis le `@Query` et ignore le body** → `updated_at` n'est PAS bumpé → merger 3-way pourrait ignorer la suppression.~~ ✅ commit `8750536` (2026-05-05, V6.1-A1) : `@Query` parasite retiré, body Kotlin Room-default préservé.
- [x] ~~**`MuscleGoalDao.deleteAll() + clearAll()` doublon strict** — même `DELETE FROM muscle_goals`. Doublon strict.~~ ✅ commit `61d507f` (2026-05-05, V6.1-A2) : `deleteAll()` supprimé, 0 callsite.
- [x] **Architecture incohérente `CycleWorkout` vs `training_cycles/{}/workouts/{}`** — 2 façons de gérer le lien planned_workout↔training_cycle. ✅ V4.1 (commit `adea775`, 2026-05-05) — 3 endpoints `/training-cycles/{}/workouts/...` (link/list/unlink) supprimés serveur après confirmation 0 callsite Android. Gestion désormais uniquement via `/cycle-workouts/{uuid}` (entité junction avec uuid, sécurisée V2.1). ~100 lignes de code mort retirées.

### 🟠 Squelette CRUD / router / schema (politique uniforme)

- [x] **Squelette CRUD canonique à appliquer aux 27 modules** — ✅ V6.2 + V6.3 (~17 commits, 2026-05-05). Sous-batch I (6 CRUDs canoniques `(db, uuid, dto, user_id)` + ownership 403 + écrasement total `model_dump` + delete bool) : muscle (`d2c2941`), routine_period (`a3c4ffc`), routine_task + routine_task_check (`87cc008`), superset_group (`20eaab8`), equipment Type C (`f7847c2`). Sous-batch I-7 (delete-fix bool 5 autres) : training_cycle, actual_workout_exercise, actual_workout_set, cycle_workout, exercise_muscle. Sous-batch II tags Swagger 20 routers (`939035c`). Sous-batch III `model_config = {"populate_by_name": True}` ajouté sur 18 `XBase` (`23a8ff3`). Sous-batch IV DAOs Style A (`54168e7`).
- [x] **`equipment_crud.bulk_upsert_equipments` typage faux** ([equipment_crud.py:43](../serveur/app/crud/equipment_crud.py#L43)) — prend `list[EquipmentOut]` au lieu de `EquipmentCreate`. ✅ commit `9531f3e` (2026-05-05)
- [x] **`actual_workout_set_crud.upsert_many_actual_workout_sets` typage faux** — prend `list[models.ActualWorkoutSet]` (modèles SQLAlchemy !) au lieu de `list[ActualWorkoutSetCreate]`. ✅ commit `4004539` (2026-05-05, refondu en délégation au singleton, retire les `.dict()` v1)
- [x] **`actual_workout_set_crud.add_set_to_actual_workout(db, set_data: dict)` sans user_id check** — ✅ V6.3 (commit `c7a1b58`, 2026-05-05) — fonction supprimée (orphelin, doublon).
- [x] **`actual_workout_exercise_crud` 3 fonctions redondantes** — ✅ V6.3 (commit `e833bcb`, 2026-05-05) — `create_*` + `update_*` retirés, gardé `upsert_*` canonique + `.dict()` → `.model_dump()`.
- [x] **`delete_X` retournent l'objet ORM** au lieu de `bool` (rupture canonique) — ✅ V6.2-I-7 (2026-05-05) — `training_cycle`, `actual_workout_exercise`, `actual_workout_set`, `cycle_workout` (renommée `remove_workout_from_cycle` → `delete_cycle_workout`), `exercise_muscle`. Routers : `response_model=XOut` retiré sur DELETE, retour `{"ok": True}` + 404 si False.
- [x] **`equipment_crud.delete_equipment` retourne dict** — ✅ V6.2-I (commit `f7847c2`, 2026-05-05) — Type C canonique, retourne bool.
- [x] **`exercise_muscle_crud.delete_exercise_muscle_by_uuid` ne retourne rien** — ✅ V6.2-I-7 (2026-05-05) — retourne bool.
- [x] **Routers utilisent `dict()` (pydantic v1)** au lieu de `model_dump()` — ✅ V6.3 (commit `e833bcb`, 2026-05-05) — `.dict()` → `.model_dump()` sur les CRUDs/routers concernés.
- [x] **`exercise_equipment_crud.upsert_exercise_equipment` modifie l'UUID d'un objet existant** — ✅ V6.3 (commit `e833bcb`, 2026-05-05) — `existing_combo.uuid = uuid` retiré.
- [x] **`muscle_router.upsert_muscle_route` parse body manuellement** ([muscle_router.py:50-62](../serveur/app/routers/muscle_router.py#L50)) — `request.json()` puis `MuscleCreate(**body)`. Solution propre : Pydantic body normal + assertion uuid path == uuid body. ✅ Résolu de fait par V6.2-I (commit `d2c2941`, 2026-05-05) — code actuel utilise `muscle: MuscleCreate` body Pydantic + `if muscle.uuid != uuid: raise HTTPException(400, "UUID path/body mismatch")`. Item ticker en F5b (2026-05-06).
- [x] **5 routers mutent le DTO (`dto.uuid = uuid`)** — ✅ V6.2-I + V6.3 (2026-05-05) — `routine_period_router`, `routine_task_router`, `routine_task_check_router`, `muscle_router` migrés en V6.2-I (passent par `crud.upsert(db, uuid, dto, user_id)`) ; `actual_workout_set_router` `dto.uuid = uuid` → `if dto.uuid != uuid: raise 400` en V6.3 (commit `c7a1b58`).
- [x] **`exercise_muscle_router` 2 DELETE qui se chevauchent** — ✅ V6.3 (commit `e833bcb`, 2026-05-05) — version par paire supprimée + `crud.delete_exercise_muscle` orphelin retiré.
- [x] **Plusieurs `delete_X` côté router** renvoient `{"detail": ...}` ou bool brut sans 404 si False : ✅ V6.2-I (`routine_period_router`, `routine_task_router`, `routine_task_check_router`) + V6.2-I-7 (`actual_workout_exercise_router`) — 4/5 alignés. **Reste** : `notification_router` à aligner.
- [x] **`training_cycle_crud` aucun check user_id sur upsert/delete/link** — ✅ V5.7-B' (commit `a01562f`, 2026-05-05) — bascule Type C → Type A user-scoped, ownership check 403 cross-user. Plus le contexte global "entité Type C" : chaque user a maintenant ses propres cycles.
- [x] **Aucun router sauf `muscle_router` n'a `tags=` ni `prefix=`** — ✅ V6.2-II (commit `939035c`, 2026-05-05) — `tags=["xxxs"]` ajouté sur 20 routers via script PowerShell. `prefix=` skipé (trop intrusif).
- [x] **`model_config` asymétrique sur `XOut` vs `XBase`** — ✅ V6.2-III (commit `23a8ff3`, 2026-05-05) — `model_config = {"populate_by_name": True}` ajouté sur 18 `XBase` + 1 manuel pour `cycle_workout_schema`.

### 🟠 Squelette syncable / API Retrofit / DAO

- [x] **`ExerciseMuscleSyncable` divergence** — ✅ V4.1 (commit `6ab569b`, 2026-05-05) — `upsert(uuid, item)` + `delete(uuid)` canoniques (côté Api + Syncable + serveur).
- [x] **`CycleWorkoutSyncable.upsert(item)` sans uuid** — ✅ V4.1 (commit `dcf4d71`, 2026-05-05) — refactor sur `upsert(uuid, item)` + `delete(uuid)`. Débloque le sync individuel CycleWorkout qui était 405 garanti.
- [x] **2 styles syntaxiques DAO co-existent** — ✅ V6.2-IV (commit `54168e7`, 2026-05-05) — `ActualWorkoutDao` + `ActualWorkoutExerciseDao` migrés Style B (`rawX`) → Style A (`XInternal`), 4 méthodes chacun, 0 callsite externe.

### 🟠 Architecture VMs / Repositories

- [x] **`MuscleGoalsManager` (data layer) importe `ui.screens.parseTargetMinimum`** — ✅ V6.4-D1 (commit `68b2b9f`, 2026-05-05) — `parseTargetMinimum` + `calculateGoalProgress` migrés vers `utils/GoalUtils.kt`. Callsites `MuscleGoalsManager` + `GoalsTabViewModel` + `GoalRow` corrigés.
- [x] **Logique dupliquée `parseTargetReps`** — ✅ V5.5/F (commit `9efe3b3`, 2026-05-05) — `parseTargetReps` locale supprimée des Home/CalendarVM (suivi de la suppression `target_reps` Room V5.2-target_reps). `targetRepsRange` désormais dérivé de `actualWorkoutExercise.reps` parsé par `parseRepsRange`.
- [x] **`MuscleGoalDao.updateStatusAccordingToDone` SQL embarque la logique métier** — ✅ V6.4-D2 (commit `c67fdc4`, 2026-05-05) — fonction supprimée (~20 lignes SQL CASE WHEN, 0 callsite).
- [x] **`ExerciseScreenViewModel:49` typage incorrect** — ✅ V6.4-D3 (commit `509a152`, 2026-05-05) — param `actualWorkoutDao: ActualWorkoutExerciseDao` orphelin retiré.
- [x] **2 ViewModels VIDES** : `StatsViewModel`, `HistoryViewModel`. ✅ B3-1 + B3-2 (2026-05-07) : `HistoryViewModel` supprimé (doublon Calendar — commit `3d206ce`). `StatsViewModel` implémenté complet via B3-2 (6 commits, écran central + 2 sous-écrans + Vico chart + indexes Room v13→v14). Cf. [TODO_FEATURES §3](TODO_FEATURES.md).
- [x] **`AuthManager.skipInterceptorAuth = true` flag jamais lu** — ✅ V6.4-D3 (commit `509a152`, 2026-05-05) — flag retiré de `RetrofitInstance` + 2 écritures mortes dans `AuthManager`.
- [x] **`AuthManager.setUserId(-1)` sentinel en mode Offline** — ✅ V6.4-D3 (commit `509a152`, 2026-05-05) — `setUserId(-1)` → `clearUserId()` (sémantique propre).
- [x] **`ChronoViewModel.onTimerFinished` hardcode `"Rest", 90`** — ✅ B4 chrono refactor (2026-05-10, commit P2.6 `e4408a3`) — résolu en passant par `TimerStateMachine.name` (label preset stocké via `setTimerDuration(name, ms)`) + `timer.durationMillis.value / 1000`. Pour les durées custom (dialog), `timerNameForDuration()` génère un nom human-readable (ex. "1 min 30 s"). Notif affiche maintenant "Timer finished — 1 min (60 s)" au lieu de "Rest (90 s)". Cf. [chrono/ui/ChronoScreenViewModel.kt](../appli-android/app/src/main/java/com/example/sportapp/chrono/ui/ChronoScreenViewModel.kt).
- [x] **`NotificationCenter.post` double-vibration potentielle** — ✅ V6.4-D4 (commit `502b408`, 2026-05-05) — bloc `VibrationUtils.vibrateForNotification` retiré du `NotificationCenter.post`, le channel système gère via `vibrationEnabled=settings.vibrateOnInAppNotification`. Param `appContext` + import cleanup.
- [ ] **Auto-completion dans `combine` `GoalsTabViewModel`** ([GoalsTabViewModel.kt:128-136](../appli-android/app/src/main/java/com/example/sportapp/viewmodel/GoalsTabViewModel.kt#L128)) — `viewModelScope.launch { dao.updateStatus(g.uuid, "DONE") }` lancé à chaque émission. Protégé par `status == "DONE" → return false` mais fragile. Refactor : auto-completion explicite à la fin d'un set.

### 🟠 TypeConverters

- [x] **TypeConverters : `Gson()` instancié à chaque conversion** — ✅ V6.5-E1 (commit `b51df9c`, 2026-05-05) — `private object ConvertersGson { val gson: Gson = Gson() }` partagé.
- [x] **TypeConverters sans try/catch JSON** — ✅ V6.5-E1 (commit `b51df9c`, 2026-05-05) — try/catch `JsonSyntaxException` sur `fromJson` (log warning + retour `null`).
- [x] **`NotificationDataConverter` bug typage Gson Int→Double** — ✅ V6.5-E1 (commit `b51df9c`, 2026-05-05) — `Notification.data: Map<String, Any>?` → `Map<String, String>?` (élimine ambiguïté Gson). 4 callsites mis à jour (NotificationRepository, NotificationCenter, PhoneNotificationManager, NotificationNavigationMapper).

### 🟠 Doublons stricts (à dédupliquer)

- [x] ~~**`ActualWorkoutSetDao` doublon strict `markAsPendingDeletion`** — `markSetsAsPendingDeletionWithUUID(uuid)` et `markAsPendingDeletion(uuid)` font la même UPDATE par uuid.~~ ✅ commit `b0d530d` (2026-05-05, V6.1-A3) : `markSetsAsPendingDeletionWithUUID` supprimé, callsite (SessionExerciseViewModel:294) migré. `markSetsAsPendingDeletionWithExerciseUUID` (filtre parent) conservée.
- [x] ~~**`MuscleDao.toggleFavorite` + `updateFavorite` doublon strict** — même UPDATE, deux noms.~~ ✅ commit `79d211b` (2026-05-05, V6.1-A4) : `toggleFavorite` supprimé (nom trompeur — la fonction prend le bool en param), callsite (MuscleScreenViewModel:36) migré sur `updateFavorite`.
- [x] ~~**`MuscleGoalDao.getAllUnSynced` (S majuscule)** — divergence du canonique `getAllUnsynced`. Renommer + chercher callsites.~~ ✅ commit `0029411` (2026-05-05, V6.1-A5) : déclaration + 2 callsites (RemoteDataUpserter, MuscleGoalSyncable) renommés, commentaire TODO retiré.

### 🟡 Layout / cosmétique architecture

- [x] **TypeConverters dans les fichiers de modèle** au lieu de `data/local/Converters.kt` dédié — ✅ V6.5-E1 (commit `b51df9c`, 2026-05-05) — `InstructionsConverter` + `NotificationDataConverter` consolidés dans `data/local/Converters.kt`.
- [x] **2 data classes "projection" dans fichiers de modèle** — ✅ V7.4-1C (commit `f4e6acf`, 2026-05-05) — `ActualWorkoutExerciseWithWorkoutDateAndSets` + `ExerciseMuscleSimple` déplacées dans `data/model/projections/`. 5 imports + retrait `Embedded` orphelin.
- [x] **`CycleWorkoutDao.getCycleWorkoutByUUID` ambigu** (`@Query` SELECT + body Kotlin `getAllOnce().find{}`) — confirmé jamais appelé MAIS fonction du squelette canonique. Garder mais clarifier (retirer le body, garder l'annotation). ✅ vague F5a-7 (2026-05-06) — body Kotlin `getAllOnce().find{}` retiré. Room génère désormais l'implémentation à partir du seul `@Query` (déjà ce qu'il faisait, le body était mort/ignoré). Plus d'ambiguïté.
- [x] **Naming class ≠ filename** — ✅ V7.4-1A (commit `b73fcd2`, 2026-05-05) — 3 VM classes renommées : `LoginViewModel` → `LoginScreenViewModel`, `ProfileViewModel` → `ProfileScreenViewModel`, `RoutineTasksViewModel` → `RoutineTasksScreenViewModel` + 11 tags Log.e alignés.
- [x] **`println(...)` partout dans les VMs** — ✅ V7.4-2 (commit `d522f31`, 2026-05-05) — 215 println → 0. Politique = DELETE pour debug noise + CONVERT en Log.d/.w/.e pour les load-bearing.
- [x] **`NotificationRepository.build()` en 2 versions** (string + enum) — ✅ V7.4-1B (commit `1cf6d7d`, 2026-05-05) — `build(String)` `@Deprecated` + ReplaceWith vers enum + `NotificationCenter.notifyTimerDone` migré `NotificationType.TIMER_DONE`.
- [x] **Routes hardcodées comme strings dans `MainActivity.NavHost`** — ✅ V7.4-3 (commit `2b664fa`, 2026-05-05) — `navigation/Routes.kt` créé centralise ~70 magic strings (19 const val + 5 PATTERN + 5 helpers + 5 ARG_*). 15 fichiers migrés.
- [x] **`session/{sessionUUID}` route Android** — ✅ V7.4-1E (commit `ce64faf`, 2026-05-05) — route legacy documentée (donnée renommée `actual_workout` côté serveur, route UI conservée pour ne pas casser les 4 callsites).

### 🟡 Architecture serveur

- [x] **`OAuth2PasswordBearer` déclaré 2 fois** — [auth.py:16](../serveur/app/auth.py#L16) + [auth_router.py:14](../serveur/app/routers/auth_router.py#L14). Factoriser : importer celui de `auth.py`. ✅ commit `fa7d36c` (2026-05-04 QW)
- [x] **`tokenUrl="token"` (relatif)** dans `OAuth2PasswordBearer` ([auth.py:16](../serveur/app/auth.py#L16)) — usuel `"/token"`. ✅ vague F5a-1 (2026-05-06) — `tokenUrl="/token"` (path absolu, convention OAuth2/Swagger).
- [x] **Erreur 404 vs 401 dans `get_current_user_id`** ([dependencies.py:16](../serveur/app/dependencies.py#L16)) — utilisateur supprimé avec token encore valide → 404, devrait être 401. ✅ Résolu de fait par V6.6-B (commit `f36b14e`, 2026-05-05) — refactor `get_current_user_id` lit le `user_id` directement du JWT (économie 1 SELECT) + 401 propre si payload sans user_id (`Token sans user_id (re-login requis)`). Plus de re-query DB → plus de cas "user supprimé". Item ticker en F5a (2026-05-06).
- [x] **`Base.metadata.create_all` + Alembic en parallèle** — duplication des sources de vérité pour le schéma. Décider : tout via Alembic OU continuer hybride mais documenter. ✅ vague F8-Q3 (2026-05-06) — option (b) retenue : hybride documenté. Politique 16 ajoutée à CLAUDE.md : `Base.metadata.create_all` autorisé uniquement dans les scripts de bootstrap/reset complet (`exec_pg.py` + `app/fill_database.py`) ; toutes les migrations incrémentales passent par Alembic (target_metadata branché V3.4 → autogenerate disponible). La Pi prod n'exécute JAMAIS `exec_pg.py` ni `fill_database.py` (déploiement = `alembic upgrade head` via `deploy.sh`). 0 changement de code (politique reflète l'état actuel : 2 callsites confirmés `exec_pg.py:14` + `fill_database.py:22`, aucun ailleurs).
- [x] **HTML inline lourd dans `main.py`** ([main.py:119-223](../serveur/app/main.py#L119)) — ~100 lignes pour `/secure-docs` et `/token-helper`. Déporter dans `app/static/secure_docs.html` + `app/static/token_helper.html`. ✅ vague F6-5 (2026-05-06) — fait. Cf. item dédié dans §6 ci-dessous.
- [x] **`pg_listener` swallow `Exception` silencieusement** — ✅ V6.6-B (commit `f36b14e`, 2026-05-05) — passé en `logger.exception(...)` explicite (logging stdlib).
- [x] **`pg_listener.py:28-29` try/catch sans log** — ✅ V6.6-B (commit `f36b14e`, 2026-05-05) — passé en `logger.warning(...)`.
- [x] **`ws_hub` itère `_clients` sans lock** ([ws_hub.py:36](../serveur/app/ws_hub.py#L36)) — pas un bug actuel (asyncio mono-thread) mais fragile à un futur changement (multiprocessing, threadpool). ✅ vague F5b-2 (2026-05-06) — analyse confirmée : `list(self._clients)` ligne 36 fait un snapshot atomique (pas d'`await` entre lecture et iter) → safe vs register/unregister concurrents (qui prennent le lock). Commentaire explicatif ajouté + flag pour reprendre si on passe à threadpool/multiprocessing.
- [x] **Pas de cap sur la queue PG** ([pg_listener.py:14](../serveur/app/pg_listener.py#L14)) — `asyncio.Queue()` sans `maxsize`. Si saturé, peut grossir indéfiniment. ✅ vague F5a-3 (2026-05-06) — `asyncio.Queue(maxsize=1000)` + `try/except asyncio.QueueFull` dans `_on_notify` qui drop le payload + warn log. Si la queue est saturée (consumer lent), perte d'event au lieu de fuite mémoire.
- [x] **`print` au lieu de `logger` partout** dans `ws_hub`, `pg_listener`, `ws_router`. Mettre en place un `logging` config configurable. ✅ vagues F5b-3 (2026-05-06) + V6.6-B (`pg_listener` déjà fait commit `f36b14e`) — sweep complet : `ws_hub.py` 3 `print` → `logger.info` ; `ws_router.py` 3 `print` → `logger.info` + 1 `logger.warning` (client_id absent). Module `logging.getLogger(__name__)` partout. **Item différé** : config `logging.basicConfig`/`logging.config.dictConfig` (politique projet à définir — passe par les loggers root par défaut FastAPI/uvicorn pour l'instant).
- [x] **Construction du DSN listener fragile** — ✅ V6.6-B (commit `f36b14e`, 2026-05-05) puis V7.5 fix runtime (commit `df28351`, 2026-05-06) — `make_url(...).set(drivername="postgresql").render_as_string(hide_password=False)`. **Bonus** : V7.5 a découvert que `str(SQLAlchemy URL)` masque le password par `***` (safe rendering SQLAlchemy 2) → push WS HS pendant ~1 jour, jamais détecté car aucun smoke runtime entre V6.6-B et V7.5.
- [x] **WS params `client` et `v` inutilisés** ([ws_router.py:13-14](../serveur/app/routers/ws_router.py#L13)) — soit utiliser pour logging/versioning, soit retirer. ✅ vague F5a-4 (2026-05-06) — les 2 params sont maintenant loggés à l'open (`logger.info("ws: open user_id=%s client=%s v=%s ...")`). Côté Android, l'URL WS inclut désormais `&v=1&` pour cohérence (avant : `client_id` mais pas `v`).
- [x] **`OkHttpClient()` WS sans timeouts customisés** ([WebSocketManager.kt:65](../appli-android/app/src/main/java/com/example/sportapp/data/remote/WebSocketManager.kt#L65)) — utilise les défauts (10s/10s/10s). ✅ vague F5a-5 (2026-05-06) — `OkHttpClient.Builder().pingInterval(30, TimeUnit.SECONDS).readTimeout(0, TimeUnit.MILLISECONDS).build()`. `pingInterval(30s)` keep-alive applicatif (évite coupures NAT/proxy + détecte clients morts). `readTimeout(0)` désactive le timeout par défaut OkHttp 10s qui coupait les WS idle.
- [x] **`Client_id` WS optionnel mais nécessaire pour l'exclude** ([ws_router.py:29](../serveur/app/routers/ws_router.py#L29)) — si client omet `?client_id=`, serveur en génère un mais ne match pas le `X-Client-Id` Android local → exclude origin cassé. Documenter (ou exiger : 401 si absent). ✅ vague F5a-6 (2026-05-06) — option "documenter + WARN" retenue (vs 401 strict). `logger.warning` si client_id absent + commentaire explicatif détaillé dans `ws_router.py` qui décrit la conséquence (exclude origin cassé pour ce client) et le fait que c'est acceptable pour un test ad-hoc tiers. Android envoie toujours `client_id` via `ClientIdProvider` — pas de regression.
- [x] **Typo dans `crud/__init__.py`** ([crud/__init__.py:95-96](../serveur/app/crud/__init__.py#L95)) — `upsert_muscle_goal` exporté **deux fois** dans le bloc `from .muscle_goal_crud import (...)`. ✅ commit `b94f9d9` (2026-05-04 QW)
- [x] **`exercise_crud.bulk_upsert_exercises` raise sur duplicate name** ([exercise_crud.py:67](../serveur/app/crud/exercise_crud.py#L67)) — comportement spécifique non aligné. Soit imposer `UNIQUE(user_id, name)` côté DB, soit étendre le squelette. ✅ vague F8-Q4 (2026-05-06) — option (a) retenue : `UNIQUE(user_id, name)` Postgres. Modèle SQLAlchemy `exercise.py` ajoute `__table_args__ = (UniqueConstraint("user_id", "name", name="uq_exercises_user_id_name"),)`. Migration Alembic `3ec81eb456a1_f8q4_unique_exercises_user_id_name` (autogenerate + apply PC dev). Check Python redondant retiré dans `bulk_upsert_exercises` (5 lignes) — squelette canonique strict, Postgres devient source de vérité. DBML aligné (`(user_id, name) [unique]`). Smoke E2E : duplicate INSERT raise UniqueViolation comme attendu. Pré-flight PC dev confirmé 0 doublon en DB avant migration.
- [x] **Pas de check d'expiration JWT côté client** — ✅ V8.2 (commits `639cc91` + `b698391` + `aaf6038` + `b338a4c`, 2026-05-06) — refresh token rotation OAuth 2.1-like + Authenticator OkHttp sous Mutex (un seul `/refresh` concurrent même avec 21 GETs parallèles). `EncryptedSharedPreferences` (AES256_GCM via Android Keystore). slowapi rate limit (5/min token, 30/min refresh, 3/h signup). UX : refresh transparent au 401, fallback logout silent si refresh KO.

---

## §6 — Code mort / Cleanup

### 🟠 Code mort confirmé

- [x] **44 endpoints `@POST` côté Android = code mort** — ✅ V7.2-C3 (commits `00bf177` + `d1dbff1`, 2026-05-05) — 38 `@POST` retirés sur 19 Apis Retrofit + 4 endpoints orphelins additionnels (`upsertByUuid`, 2× `getByUUID`, `deleteAll`).
- [x] **`MuscleWeeklySummary` entité fantôme à supprimer** — ✅ V5.1-G (4 commits, 2026-05-05) — cascade complète : Alembic `20260505_v5_1_drop_mws` + 5 fichiers serveur supprimés + 11 modifiés (`__init__.py` ×4, `main.py`, `triggers_loader.py` PER_TABLE_FRAGMENTS 21→20, `db_triggers/user_id_helper.sql`, `seed_database.py`, `view_database.py`, `clear_database.py`) + diagrams (`a461401`) + 5 fichiers Android supprimés + 9 modifiés + Room v9→v10 `MIGRATION_9_10` (DROP TABLE). Smoke E2E concluant.
- [x] **Endpoints Android orphelins (4)** — ✅ V7.2-C3 (commits `00bf177` + `d1dbff1`, 2026-05-05) — 4 orphelins supprimés.
- [x] **Endpoints serveur orphelins** — partiellement résolu : 3 endpoints `/training-cycles/{cycle_uuid}/workouts/...` (link/list/unlink) supprimés en V4.1 (commit `adea775`). ✅ vague F10 (2026-05-06) — audit cas par cas terminé sur les endpoints listés : (1) **`PUT /users`** sans id : item obsolète, déjà retiré V1.1 (commentaire `user_router.py:22-23` le confirme — création passe par `POST /signup` public). Tic. (2) **`GET /muscle-goals/{uuid}`** : 0 callsite Android (MuscleGoalApi.kt n'a pas de `getByUUID`) **mais conservé** car partie du squelette canonique CRUD (politique CLAUDE.md §9 : "fonctions du squelette à conserver même si pas utilisées"). Le `read_X` canonique sert de fondation pour de futurs callers. (3) **`GET /superset-exercises/group/{group_uuid}`** : 0 callsite Android + non-canonique → **supprimé** (router + CRUD `get_superset_exercises_by_group_uuid` + export `crud/__init__.py`). (4) **`GET /planned-workout-exercise/by-uuid/{exercise_uuid}`** (singulier non-canonique) : 0 callsite Android + path non-conforme squelette → **renommé** `GET /planned-workout-exercises/{uuid}` (canonique pluriel + param `uuid`), fonction `get_exercise_by_uuid` → `read_planned_workout_exercise`. **Bonus identifié à l'audit** : `GET /planned-workouts/{planned_workout_uuid}/exercises` également supprimé (0 callsite Android + non-canonique) + CRUD `get_exercises_for_planned_workout_uuid` retiré (1 seul callsite était l'endpoint orphelin) + export `crud/__init__.py`. **Endpoints PUT/DELETE `/exercise-muscles/{uuid}` + `/cycle-workouts/{uuid}`** confirmés gardés (Android utilise depuis V4.1, signature canonique, déjà tickered). Smoke import serveur OK + listing routes confirme squelette aligné sur 5 endpoints canoniques (getAll, getByUUID, bulk, upsert, delete) pour les 2 routers touchés.
- [x] **`MuscleGoalApi.post(@Body goal): Long`** — ✅ V7.2-C3 (2026-05-05) — supprimé avec le sweep `@POST`.

### 🟡 Code mort à confirmer

- [x] **`old_exec_file.py`** ([serveur/old_exec_file.py](../serveur/old_exec_file.py)) — code mort confirmé. À supprimer. ✅ commit `a0e6afc` (2026-05-04 QW)
- [x] **`/secure-docs` HTML inline dans `main.py`** (~100 lignes HTML/JS) — extraire dans `app/static/secure_docs.html` ou supprimer. ✅ vague F6-5 (2026-05-06) — extraction effectuée. 2 fichiers créés : `app/static/secure_docs.html` (Swagger UI custom + token via postMessage) + `app/static/token_helper.html` (form login → POST /token → ouvre /secure-docs). `main.py` lit les fichiers via `Path(__file__).parent / "static" / "..."` + `read_text(encoding="utf-8")`. ~100 lignes inline retirées de `main.py`. Pas de `StaticFiles` mount (les pages sont publiques par design).
- [x] **CORS `allow_origins` pour `localhost:4200`** — ✅ V7.2-C1 (commit `b39142c`, 2026-05-05) — `CORSMiddleware` + `ALLOWED_ORIGINS` retirés (Android-only via Retrofit, CORS jamais évalué).
- [x] **`resolve_user_id_from_token` jamais utilisé** — ✅ V7.2-C1 (commit `b39142c`, 2026-05-05) — helper supprimé.
- [x] **Imports inutilisés `database.py`** ([database.py:3](../serveur/app/database.py#L3)) — `sessionmaker` (sync) importé jamais utilisé. ✅ commit `b5f7f9d` (2026-05-04 QW)
- [x] **`getTodayStartWithTimezone` produit format Postgres legacy avec espace** ([CustomDateUtils.kt:33-40](../appli-android/app/src/main/java/com/example/sportapp/utils/CustomDateUtils.kt#L33)) **mais n'est appelée nulle part**. Code mort à supprimer. ✅ commit `561ba03` (2026-05-04 QW)
- [x] **Bloc `WorkoutSession` commenté dans `seed_database.py`** — ✅ V7.2-C1 (commit `b39142c`, 2026-05-05) — supprimé.
- [x] **`view_database.py` référence `MuscleWeeklySummary`** — ✅ V5.1-G (commit `dc84efe`, 2026-05-05) — référence retirée avec la suppression de l'entité.
- [x] **`notify_training_cycles_change()` fonction définie jamais appelée** — ✅ V4.3 (commit `7fa8c9e`, 2026-05-05) — `training_cycles_trigger.sql` LEGACY migré en fragment moderne.
- [x] **`MuscleWeeklySummarySyncHandler` toujours injecté dans `WebSocketManager`** — ✅ V5.1-G (commit `7465608`, 2026-05-05) — handler supprimé + `WebSocketManager.kt` retire handler + branche `when`.
- [x] **`User.firstName` / `User.lastName` champs Room non utilisés** (0 occurrence UI/VMs) **et non renvoyés par `/me`**. Soit supprimer côté Room (avec migration), soit étendre `UserOut` côté serveur + afficher dans `ProfileScreen`. ✅ vague F6-4 (2026-05-06) — option "étendre UserOut" retenue : `first_name` + `last_name` ajoutés à `UserOut` (avec aliases camelCase). Sont déjà saisis au signup V8.3 (form 5 champs) + stockés Postgres `users.first_name/last_name`, désormais re-lus côté client via `/me`. **Affichage UI ProfileScreen différé** (feature B mineure, hors scope F6).

### ⚪ Cleanup mineur

- [x] **`serveur/readme.md`** — ✅ V7.3 (commit `77d31a3`, 2026-05-05) — 53 lignes legacy → 19 lignes pointer DEV_GUIDE + quickstart.
- [x] **`appli-android/exportToHTML/`** — ✅ V7.3 (commit `77d31a3`, 2026-05-05) — dossier local supprimé.
- [x] **`StorageManager` ne logge pas si image corrompue** — ✅ V7.4-1D (commit `e772aca`, 2026-05-05) — `Log.w` ajouté si `BitmapFactory.decodeFile`/`decodeStream` retourne null.
- [x] **`RemoteDataGetter` notifications `insertAll` manquant** ([RemoteDataGetter.kt:193-196](../appli-android/app/src/main/java/com/example/sportapp/sync/RemoteDataGetter.kt#L193)) — code mort de toute façon. ✅ vague F6-2 (2026-05-06) — bloc `notifications` aligné sur les 20 autres entités : `notificationDao.clearAll()` + `notificationDao.insertAll(notifications.map { it.copy(synced = syncedAfter) })` ajoutés. Le `helper fetchAllAndPersist(syncedAfter, log)` (F1+C) traite désormais l'entité 21/21 cohérente.

---

## §7 — Documentation

### 🟠 Documentation à régénérer / corriger

- [x] **~~`routes.json` obsolète~~** — ✅ V7.3 (2026-05-05) : supprimé. Source vivante = Swagger `/secure-docs`.
- [x] **~~`test_api.py` obsolète~~** — ✅ V7.3 (2026-05-05) : `tests/full_test.py` (qui le générait) supprimé en bloc avec `routes.json`.
- [x] **`alembic/env.py:target_metadata = None`** ([alembic/env.py:28](../serveur/app/alembic/env.py#L28)) — empêche `alembic revision --autogenerate`. Brancher : `from app.database import Base; import app.models; target_metadata = Base.metadata`. ✅ commit `b0546b8` (2026-05-05)
- [x] **Politique de traçabilité des changements** (mémoire 2026-05-04) — convention à appliquer : tout changement code/schéma doit être reflété dans la doc associée (DBML, CLAUDE.md, DEV_GUIDE.md, routes.json, etc.). ✅ vague F6-6 (2026-05-06) — politique formalisée dans `CLAUDE.md §14` : sources de vérité listées (DBML = intention, models SQLAlchemy = effectif, DATABASES.md = audit, SERVEUR.md §3 = inventaire types, CLAUDE.md historique = chronologie). Convention "même commit pour code+doc" + Room schemas JSON auto-générés par KSP à committer.

### 🟡 Doc cosmétique

- [x] **~~`alembic.ini.sqlalchemy.url` placeholder~~** — ✅ V7.3 (2026-05-05, commit `77d31a3`) : commentaire explicatif ajouté (valeur ignorée, env.py lit settings.DATABASE_URL). Décision : garder default upstream pour ne pas masquer une vraie URL avec un faux mdp.
- [x] **~~Revision ID Alembic placeholder `2025xxxx_notify_triggers`~~** — ✅ V7.3 (2026-05-05, commit `77d31a3`) : commentaire historique ajouté. Renommer casserait `alembic upgrade head` sur PC dev + Pi prod tant qu'un `UPDATE alembic_version` n'est pas appliqué manuellement. Coût > bénéfice cosmétique.
- [x] **`exportSchema = false` sur Room** — ✅ V3.1 (commit `2439fd1`, 2026-05-05) — `exportSchema = true` activé en même temps que la mise en place du système de migrations Room (schémas JSON v6/v7 commités).
- [x] **~~`fill_database.py` n'attache pas les triggers~~** — ✅ V7.3 (2026-05-05, commit `77d31a3`) : `conn.execute(text(attach_triggers_sql()))` ajouté après `Base.metadata.create_all`. Triggers idempotents.

### ⚪ Doc tests

- [x] **`seed_database.py` ne crée que 5 muscles** ([seed_database.py:423-446](../serveur/app/seed_database.py#L423)) — la liste `names_zones` ouvre une string `"""` après les 5 premiers items, transformant les 20 muscles suivants en commenté Python. Retirer les `"""`. Vérifier `m_uuids` cohérent avec `range(N)`. ✅ vague F6-1 (2026-05-06) — bug confirmé : `"""` parasites + 20 tuples morts + `m_uuids = [... range(25)]` incohérent (seuls 5 indices utilisés par les ExerciseMuscle/MuscleGoal). Décision cleanup complet : 22 lignes mortes retirées, `m_uuids = [... range(5)]`, `for i in range(len(names_zones))` (cohérent avec la liste de 5 tuples). Comment de tête ajouté pointant `starter_pack.py` pour le catalogue 12 muscles V8.4.

---

## §8 — Tests existants

### 🟠 Audit fichiers de tests

- [x] **~~Auditer les fichiers de tests existants~~** — ✅ V7.3 (2026-05-05) : seul `serveur/tests/full_test.py` existait (les autres `test_api.py`, `test_db_connection.py` cités en audit n'étaient plus présents au moment du fix). `full_test.py` utilisait HTTP Basic Auth (obsolète depuis V1.3 JWT) et générait `routes.json` + `test_api.py`. Les 3 fichiers supprimés. Pour la couverture future, voir TODO_FEATURES "Plan de tests".
- [x] **~~`tests/full_test.py` smoke trop lâche~~** — ✅ V7.3 (2026-05-05) : fichier supprimé en bloc avec `routes.json`.

### 🟡 Outils test

- [x] **`inspect_schema.py`** ([app/inspect_schema.py:5](../serveur/app/inspect_schema.py#L5)) — `inspect(engine)` synchrone sur engine async. À tester ; si KO, refactor (`async with engine.connect() as conn: ... await conn.run_sync(lambda c: inspect(c))`) ou créer un engine sync séparé pour ce script. ✅ vague F6-3 (2026-05-06) — refactor en `async with engine.connect() as conn: await conn.run_sync(_collect_schema)`. `_collect_schema` exécute `inspect(sync_conn)` synchrone à l'intérieur d'un thread géré par SQLAlchemy. `main()` wrap par `asyncio.run()`. Bonus : retrait des emoji `📋`/`🔹` qui crashaient sur stdout cp1252 Windows. Smoke confirme 23 tables affichées correctement.

---

## §9 — UX

### 🟠 Messages utilisateur

- [x] **`NetworkMonitor.onLost` affiche "Token expiré !"** ([NetworkMonitor.kt:78](../appli-android/app/src/main/java/com/example/sportapp/utils/NetworkMonitor.kt#L78)) — UX trompeur (c'est le réseau, pas le token). Remplacer par "Hors ligne" / "Connexion perdue". ✅ commit `24fd3d9` (2026-05-04 QW)
- [x] **22 snackbars potentiels par `syncAll`** (1 par entité via `safe*WithSnackbar`) — UX bruyante. Remplacer par 1 snackbar global "Sync OK" / "Sync : X errors". ✅ B2 (2026-05-07) — résolu **par T4.2** (refactor sync layer 2026-05-07) qui a remplacé tous les callsites `safe*WithSnackbar` dans les 19 ViewModels par `syncEngine.pushEntityClass(...)` (silencieux). Le seul snackbar global vit dans `SyncManager.syncAllToServer()` : 1 message "Starting..." + 1 message "completed" / "Error" (cf. [SyncManager.kt:51-101](../appli-android/app/src/main/java/com/example/sportapp/sync/SyncManager.kt#L51)). B2 cleanup = suppression des 2 fonctions orphelines `safeSyncEntityWithSnackbar` + `safeSyncEntityDeletionsWithSnackbar` dans [EntitySyncUtils.kt](../appli-android/app/src/main/java/com/example/sportapp/sync/base/EntitySyncUtils.kt) (62 lignes mortes + 4 imports). 42 tests JVM verts.
- [x] **`ChronoViewModel.onTimerFinished` notif "Rest 90s"** — peu importe le timer réel (cf. §5). Bug UX direct. *(différé refactor Vague 8 — chrono refactor B4)* ✅ doublon strict avec §5 ligne 308 (même bug `ChronoViewModel.onTimerFinished` hardcode `"Rest", 90`). Tickered ici en F11-cleanup (2026-05-06) pour éviter de le compter 2 fois ; la résolution effective viendra avec B4 chrono refactor.
- [x] **Pas de gestion 401 proactive Android** — ✅ V4.5 (commits `0e8d592` + `44cefe3` + `d349f0d`, 2026-05-05) — `Authenticator` OkHttp central : sur 401 → clear token+userId, set `_isTokenValid=false`, émet `SyncEvents.onTokenExpired` → snackbar "Session expirée" + nav login. Étendu V8.2 avec refresh token transparent.
- [ ] 🟡 **`DrawerMiniProgress` track invisible — fix attendu : passer à `bgSurface`** ([DrawerMiniProgress.kt:58](../appli-android/app/src/main/java/com/example/sportapp/ui/components/drawer/DrawerMiniProgress.kt#L58)) — `.background(appColors.bgRecessed, ...)` mais le parent `DrawerSection` est aussi `appColors.bgRecessed` → la portion incomplète est invisible (même couleur sur même couleur). Découvert build M11 Figma 2026-05-23. **User validé 2026-05-23 (final) : la track DOIT passer à `appColors.bgSurface`** (boxBlue plus clair) pour rendre visible la portion non-remplie. Figma M11 `State=WithProgress` corrigé en `bg/surface`. À appliquer côté code : `DrawerMiniProgress.kt:58` `.background(appColors.bgSurface, shape = RoundedCornerShape(2.dp))`.

---

## §10 — Performance

### 🟡 Indexes manquants

- [x] **Aucun index Postgres sur `user_id`** — queries fréquentes `WHERE user_id = ?` font seq scan. Ajouter `Index('ix_<table>_user_id', user_id)` sur les 12 tables Type A. Coût marginal, gain perfo significatif au-delà de quelques milliers de lignes. ✅ vague F4c (2026-05-06) — 12 indexes `ix_<table>_user_id` posés via migration Alembic `1710e67a118b_add_indexes_user_id_and_fk_uuid` (autogenerate, target_metadata branché V3.4). `index=True` ajouté sur les Column SQLAlchemy.
- [x] **Aucun index Postgres sur `<parent>_uuid`** — Postgres ne crée pas d'index automatique sur le côté `referencing` d'une FK. Queries `WHERE actual_workout_uuid = ?` (très fréquentes) font seq scan. Ajouter explicitement. ✅ vague F4c (2026-05-06) — 16 indexes `ix_<table>_<parent>_uuid` posés dans la même migration. Total 28 nouveaux indexes (12 user_id + 16 <parent>_uuid). Smoke `pg_indexes` confirme 49 indexes `ix_*` (21 anciens id/refresh + 28 nouveaux).
- [x] **`Notification` indices manquants** ([Notification.kt:13-14](../appli-android/app/src/main/java/com/example/sportapp/data/model/Notification.kt#L13)) — pas d'index sur `created_at` ni `(user_id, created_at)` alors que `observeAll() ORDER BY created_at DESC` est l'observe principal et `observeUnreadCount` filtre par `read_at IS NULL` + `pendingDeletion = 0`. Cf. aussi DBML qui annonce ces indexes mais non implémentés. ✅ vague F4d (2026-05-06) — `@Index(value = ["created_at"])` + `@Index(value = ["user_id", "created_at"])` ajoutés à `Notification.kt`. Migration Room v10→v11 (`MIGRATION_10_11`) qui pose les 2 indexes via `CREATE INDEX IF NOT EXISTS`. Naming aligné sur le pattern Room/KSP `index_<table>_<col>[_<col2>]`.
- [x] **`User` sans index sur `username`** ([User.kt](../appli-android/app/src/main/java/com/example/sportapp/data/model/User.kt)) — query `WHERE username = ?` lente. Ajouter `@Index(value = ["username"], unique = true)`. ✅ vague F4d (2026-05-06) — `@Index(value = ["username"], unique = true)` ajouté + import `androidx.room.Index`. Inclus dans `MIGRATION_10_11` via `CREATE UNIQUE INDEX IF NOT EXISTS index_users_username`. Bénéficie à `require_admin` lookup et auth_router login.

---

## Mapping vers les étapes de l'audit

> Pour suivre la traçabilité de la découverte de chaque finding.

| Étape | Sections concernées | Items clés |
|---|---|---|
| **1** (cartographie) | §1, §2, §6, §7 | Mots de passe `change-me`, `MYSECRET123`, Room sans Migration, `--reload` prod, `MuscleWeeklySummary` fantôme |
| **2A** (serveur infra) | §1, §3, §5, §7 | `/me` email synthétique, `ws_router` 500, `ws_hub` broadcast à tous, `pg_listener` swallow, HTML inline |
| **2B-2** (CRUDs/routers/schemas) | §1 (gros), §5 (gros) | Routers publics (4), bypass ownership, signatures sans uuid, squelette CRUD non respecté |
| **2C** (scripts DB + Alembic + triggers) | §3, §4, §7 | 5 entités sans push WS, 3 triggers sans userId, divergence Alembic, seed buggé |
| **3A** (Android infra) | §1, §3, §5 | `UserInfo.role` mismatch, `CycleWorkoutApi` endpoints inexistants, payload WS divergence, `MuscleGoalsManager` ui→data, WS sans else |
| **3B** (Android sync) | §3, §5 | Ordre Phase 1 deletes, handlers cross-user, RemoteDataGetter synced=false, mergeAllFromServer ordre, isRemoteNewer, isSyncing Boolean |
| **3C** (Android UI/VMs) | §5, §9 | `ExerciseScreenViewModel` typage, 2 VMs vides, `AuthManager.skipInterceptorAuth`, ChronoViewModel hardcode, parseTargetReps dupliqué |
| **3D** (Room models + DAOs) | §4, §5 | `RoutineTaskCheckDao` viole 2 couches, `SupersetGroup/Exercise` nullables, `ActualWorkoutSetDao` Style C, `CycleWorkoutDao` @Query+body, doublons, colonnes camelCase |
| **4** (DB Postgres + Pydantic + Room) | §3, §4 | 3 triggers sans userId (précisé), `target_reps` fantôme, `training_cycles` LEGACY, divergence Alembic confirmée, nullable manquants, deleted_at jamais posé |
| **4.5** (dates) | §3 | `isRemoteNewer` faux, 6 fonctions Instant.parse crash, `date()` SQLite, 3 formats wire |
| **5** (intégration) | §3, §5, §6 | CycleWorkout endpoints cassés appelés, ExerciseMuscle PUT par paire, SupersetExercise type mismatch, 44 POST code mort, mergeAllFromServer après login, gestion 401 |

---

*TODO_FIXES.md refondu lors de l'étape 6 (2026-05-04). 162 items après dédoublonnage. Voir [TODO_FEATURES.md](TODO_FEATURES.md) pour les nouvelles fonctionnalités / améliorations identifiées. Étape suivante : 7 (REVIEW.md — plan d'amélioration classé par criticité, basé sur ce TODO_FIXES + TODO_FEATURES).*
